% SPDX-License-Identifier: Apache-2.0
% Copyright (c) 2026 Mike Mol
%
% The warrant set for mikemol-fence. ONE ENTRY PER TEST FUNCTION, and the gate enforces the
% 1:1 against `grep -cE '^(    )?def test_'` over tests/.
%
% TRANSCRIBED, NOT AUTHORED: each claim is the first paragraph of its test's own docstring,
% because the prose was written at the moment the defect was measured. A thin docstring
% yields a thin claim -- a fact about the case rather than licence to invent one.
%
% THIS DISTRIBUTION'S TESTS ARE CLASS METHODS, and that is why the ledger was empty until
% 2026-09-10: both counting sites read `^def test_` anchored at column 0, so the harness
% counted ZERO here and an empty ledger satisfied 1:1 while 33 real tests went unwarranted.
% The predicate was widened rather than the tests restyled -- the classes group meaningfully
% and the instrument was the thing that could not see them.

@misc{fence-caps-root-cgroup-line-is-the-hierarchy-root,
  section = {caps},
  claim   = {`0::/` names the root itself, not a child of it.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_a_root_cgroup_line_is_the_hierarchy_root}
}

@misc{fence-caps-root-has-no-parent-inside-the-hierarchy,
  section = {caps},
  claim   = {⚑ AT THE ROOT, `.parent` ESCAPES THE CGROUP TREE — a property of the path, not a host.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_the_root_has_no_parent_inside_the_hierarchy}
}

@misc{fence-caps-nested-cgroup-line-keeps-its-parent-inside,
  section = {caps},
  claim   = {⚑ THE POSITIVE CONTROL: a normal pod path has a parent that IS a cgroup directory.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_a_nested_cgroup_line_keeps_its_parent_inside}
}

@misc{fence-caps-no-caps-is-observe-only,
  section = {caps},
  claim   = {An empty `Caps` imposes nothing.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_no_caps_is_observe_only}
}

@misc{fence-caps-any-cap-is-not-observe-only,
  section = {caps},
  claim   = {Any single cap set takes the run out of observe-only.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_any_cap_is_not_observe_only}
}

@misc{fence-caps-swap-zero-is-a-cap-not-an-absence,
  section = {caps},
  claim   = {Reject the falsy reading of `swap="0"`.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_swap_zero_is_a_cap_not_an_absence}
}

@misc{fence-caps-observe-only-still-needs-controllers,
  section = {caps},
  claim   = {Require memory+pids even when nothing is capped.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_observe_only_still_needs_controllers}
}

@misc{fence-caps-io-cap-adds-its-controller,
  section = {caps},
  claim   = {An io cap pulls the io controller into the requirement.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_io_cap_adds_its_controller}
}

@misc{fence-caps-sizes,
  section = {caps},
  claim   = {Each suffix multiplies, and a bare integer passes through.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_sizes}
}

@misc{fence-caps-max-passes-through,
  section = {caps},
  claim   = {`max` is the kernel's spelling for no-limit and must not become a number.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_max_passes_through}
}

@misc{fence-caps-nothing-bound,
  section = {caps},
  claim   = {Clean counters name no binding cap.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_nothing_bound}
}

@misc{fence-caps-memory-oom-kill,
  section = {caps},
  claim   = {An oom_kill names the memory cap.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_memory_oom_kill}
}

@misc{fence-caps-memory-throttle-without-kill-still-counts,
  section = {caps},
  claim   = {Count limit hits without a kill as the memory cap binding.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_memory_throttle_without_kill_still_counts}
}

@misc{fence-caps-throttle-and-a-kill-do-not-render-the-same-verdict,
  section = {caps},
  claim   = {⚑⚑⚑ ONE LABEL OVER TWO OUTCOMES, ON A SWAP-BACKED HOST.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_a_throttle_and_a_kill_do_not_render_the_same_verdict}
}

@misc{fence-caps-throttle-names-itself-a-throttle,
  section = {caps},
  claim   = {⚑ THE READER'S QUESTION IS `was my workload held down`, and only this answers it.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_a_throttle_names_itself_a_throttle}
}

@misc{fence-caps-kill-names-itself-a-kill,
  section = {caps},
  claim   = {⚑ THE POSITIVE CONTROL: renaming the throttle must not rename the kill.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_a_kill_names_itself_a_kill}
}

@misc{fence-caps-pids,
  section = {caps},
  claim   = {A pids.events max hit names the pids cap.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_pids}
}

@misc{fence-caps-both,
  section = {caps},
  claim   = {Two caps binding are reported as two findings, not one.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_both}
}

@misc{fence-caps-unreadable-counters-bind-nothing,
  section = {caps},
  claim   = {An empty dict means the question could not be asked, which must not read as a trip.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_caps.py -k test_unreadable_counters_bind_nothing}
}

@misc{fence-cli-separator-is-stripped,
  section = {cli},
  claim   = {The `--` separator does not reach the payload.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_separator_is_stripped}
}

@misc{fence-cli-caps-parse,
  section = {cli},
  claim   = {Cap flags land on their fields, with `--pids` typed as an int.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_caps_parse}
}

@misc{fence-cli-observe-defaults-off,
  section = {cli},
  claim   = {`--observe` is opt-in; its absence is not a cap.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_observe_defaults_off}
}

@misc{fence-cli-no-command-is-refused,
  section = {cli},
  claim   = {A fence with nothing to fence is a usage error, not an empty success.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_no_command_is_refused}
}

@misc{fence-cli-observe-refuses-to-combine-with-a-cap,
  section = {cli},
  claim   = {Refuse rather than resolve an incoherent instruction.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_observe_refuses_to_combine_with_a_cap}
}

@misc{fence-cli-observe-alone-is-accepted-by-the-parser,
  section = {cli},
  claim   = {The T-arm: `--observe` with no cap parses cleanly.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_observe_alone_is_accepted_by_the_parser}
}

@misc{fence-fence-measures-without-capping,
  section = {fence},
  claim   = {A no-caps run completes, is marked observe-only, and binds nothing.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_measures_without_capping}
}

@misc{fence-fence-peak-is-read-and-is-not-zero,
  section = {fence},
  claim   = {Charge real pages to the fence, or report None.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_peak_is_read_and_is_not_zero}
}

@misc{fence-fence-events-are-present-and-clean,
  section = {fence},
  claim   = {An uncapped run kills nothing.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_events_are_present_and_clean}
}

@misc{fence-fence-nonzero-propagates,
  section = {fence},
  claim   = {A failing payload's code is the fence's code.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_nonzero_propagates}
}

@misc{fence-fence-missing-command,
  section = {fence},
  claim   = {A command that does not exist is 127, the shell's own spelling.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_missing_command}
}

@misc{fence-fence-failed-payload-is-still-measured,
  section = {fence},
  claim   = {Measure the runs most worth measuring.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_a_failed_payload_is_still_measured}
}

@misc{fence-fence-tight-memory-cap-binds,
  section = {fence},
  claim   = {A 256MB allocation under an 8MB cap trips the memory cap and says so.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_a_tight_memory_cap_binds}
}

@misc{fence-fence-same-payload-passes-uncapped,
  section = {fence},
  claim   = {The T-arm: the identical command completes uncapped.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_the_same_payload_passes_uncapped}
}

@misc{fence-cli-the-binding-rung-is-named-in-the-payload,
  section = {cli},
  title   = {The binding rung is named in the payload},
  note    = {The answer a ratchet sweep exists to produce lived only in prose on a stream nobody
            collects. Reported from the consuming side by a peer running these fences as a host:
            they tracked out-of-memory kills for four ticks and filed a load incident, then read
            the enforced limits out of the kernel's own dump records and recognised a clean
            descending ladder, which is a sweep doing exactly what it is designed to do. Their
            measurement of this tool's own voice was zero summary lines in twenty-four hours,
            against thirty-four journal lines mentioning the fence that were all the kernel's
            records rather than ours. The gap is narrower and sharper than the information being
            missing: the per-rung binding field was already in the payload, and what was absent is
            the synthesis naming which rung bound, the one thing a sweep computes. A consumer
            received the ladder and had to re-derive the answer the tool had already rendered for
            humans. Measured before the repair, a sweep emitted a bare array with no synthesis
            field anywhere.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_the_binding_rung_is_named_in_the_payload}
}

@misc{fence-cli-nothing-binding-reports-null-rather-than-omitting-the-key,
  section = {cli},
  title   = {Nothing binding reports null rather than omitting the key},
  note    = {Absent and null are different claims to a parser. A missing key reads as this tool
            does not report that, and a null reads as it reports there was none: the first sends a
            consumer looking for another source, which is precisely the trip the reporting peer
            made to the kernel's dump records, and the second closes the question. This is the
            absence-versus-unavailable distinction this repository draws everywhere else, arriving
            at a serialisation boundary, where it costs one key to state and a reader's whole
            investigation to omit.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_nothing_binding_reports_null_rather_than_omitting_the_key}
}

@misc{fence-cli-every-rung-survives-beside-the-synthesis,
  section = {cli},
  title   = {Every rung survives beside the synthesis},
  note    = {The control, and the ladder is not replaced by its summary. A payload carrying only
            the answer would be smaller and worse, because the rungs are what let a reader check
            the synthesis rather than trust it, and the reporting peer reconstructed an entire
            sweep from rung-level evidence alone before this tool told them anything. Summarising
            is the repair; summarising instead would be the truncation defect this tree has
            already measured in its own test evidence, where a five-element slice withheld the
            scale of a finding at the moment scale was the finding.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_every_rung_survives_beside_the_synthesis}
}

@misc{fence-cli-the-payload-the-sweep-emits-is-the-one-with-the-answer,
  section = {cli},
  title   = {The payload the sweep emits is the one with the answer},
  note    = {The three arms beside this one test a function; this one tests that the function is
            called. Measured, and it is why the arm exists: reverting the emit line to the bare
            array left all three green, because they exercise the payload builder directly and an
            unwired payload — the synthesis computed and then not shipped — satisfies every one of
            them. That is the packager-is-not-a-user defect appearing inside the arms written to
            close a wiring gap, on the same tick. So this one drives the reporter and parses real
            standard output, because the subject is what a consumer receives and nothing short of
            reading the stream measures it: the reporting peer did not lack a function, they
            lacked a line on a stream they collect. It also asserts the stream split, since a
            payload landing on the narration stream would satisfy a looser check while breaking
            the contract a caller relies on when piping output into a parser. The shape is checked
            before it is indexed, because the defect it catches is a payload that is a bare list,
            and indexing one by name raises a type error that hands the reader a crash instead of
            the finding — measured on the falsification arm, which reported list indices must be
            integers rather than naming the missing answer.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_cli.py -k test_the_payload_the_sweep_emits_is_the_one_with_the_answer}
}

@misc{fence-core-the-sweep-stops-at-the-first-cap-that-binds,
  section = {fence},
  title   = {The sweep stops at the first cap that binds},
  note    = {The stopping rule is the whole point of a ratchet, and nothing asserted it. A
            mutation sweep found the loop unexercised: replacing the sweep function's body with a
            raise changed no test verdict, the only survivor in more than fifty def-sites across
            two distributions. The arms that existed tested the REPORTER, with hand-built results,
            so the function that walks the ladder was never called — the same shape as the finding
            one layer over in the markdown tool, where five modes were registered, documented,
            gate-checked and never run as programs. A sweep that ran every step would report the
            tightest cap as binding rather than the first, and those are different answers: the
            first is the scale of the resource the command needs, the tightest is only the end of
            the list the caller happened to type. No delegated subtree is needed to check any of
            this, because the subject is the loop rather than the fence, and injecting the runner
            is what separates the two questions.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_the_sweep_stops_at_the_first_cap_that_binds}
}

@misc{fence-core-the-sweep-returns-every-step-not-only-the-verdict,
  section = {fence},
  title   = {The sweep returns every step, not only the verdict},
  note    = {The caps that did not bind are what establish that the binding one is a boundary
            rather than an isolated failure. The function's own docstring states it — a caller
            handed only the last result cannot tell that it bound at one cap from that it fails at
            every cap — and nothing asserted the sequence survived. This matters beyond the
            library, because the command line publishes that sequence to consumers as the rungs
            field of its machine-readable payload, and a peer running these fences reconstructed a
            whole ladder from kernel records precisely because the tool's own account of it was not
            reaching them.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_the_sweep_returns_every_step_not_only_the_verdict}
}

@misc{fence-core-a-sweep-that-never-binds-runs-every-step,
  section = {fence},
  title   = {A sweep that never binds runs every step},
  note    = {The control, without which the stopping arm beside it is satisfied by a loop that
            always stops after one step. It pins that the sweep stops only when something binds,
            and it is also the honest-negative case the reporter renders as completed within all
            caps: either the command is frugal, or the mechanism being ratcheted is not the
            resource that constrains it. A ratchet with no binding cap is a real and reportable
            outcome rather than a failure to measure, which is why the exit code stays zero.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_a_sweep_that_never_binds_runs_every_step}
}

@misc{fence-core-the-base-caps-carry-through-every-step,
  section = {fence},
  title   = {The base caps carry through every step},
  note    = {Only the memory cap ratchets; the other caps are the invariant the sweep measures
            against. A ladder that dropped a swap setting between steps would change what a memory
            cap MEANS halfway down — a throttle at one rung and a kill boundary at the next — so
            the binding cap it reported would not be a measurement of one thing. The tool's own
            refusal text advises callers to set swap to zero to make the cap a kill boundary, and
            that advice is only sound if the setting survives every rung. The arm asserts a step
            ran before asserting anything about the steps, because an empty sequence would satisfy
            a universally-quantified claim over it while measuring nothing.},
  check   = {cmd:.venv/bin/python3 -m pytest tests/test_fence.py -k test_the_base_caps_carry_through_every_step}
}

@misc{fence-admit-ledger-round-trips-through-its-text,
  section = {admit},
  title  = {Rendering then parsing yields the same snapshot, a spaced label included},
  claim  = {Rendering then parsing yields the same snapshot, a spaced label included.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_ledger_round_trips_through_its_text},
}

@misc{fence-admit-torn-line-is-skipped-not-fatal,
  section = {admit},
  title  = {A malformed lease reads as absent; its well-formed neighbour is kept},
  claim  = {A malformed lease reads as absent; its well-formed neighbour is kept. THE CONTROL IS THE
            KEPT NEIGHBOUR — an empty parse cannot pass this arm.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_torn_line_is_skipped_not_fatal},
}

@misc{fence-admit-ledger-with-no-total-parses-as-none,
  section = {admit},
  title  = {No TOTAL_MB line is None — distinct from a total of zero},
  claim  = {No TOTAL_MB line is None — distinct from a total of zero.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_ledger_with_no_total_parses_as_none},
}

@misc{fence-admit-request-over-the-total-is-impossible-not-blocked,
  section = {admit},
  title  = {`mb > TOTAL_MB` refuses at once — waiting cannot make it fit},
  claim  = {`mb > TOTAL_MB` refuses at once — waiting cannot make it fit.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_request_over_the_total_is_impossible_not_blocked},
}

@misc{fence-admit-requests-that-fit-are-admitted,
  section = {admit},
  title  = {Two leases summing to the total leave room for neither more nor less},
  claim  = {Two leases summing to the total leave room for neither more nor less.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_requests_that_fit_are_admitted},
}

@misc{fence-admit-request-over-the-free-budget-blocks,
  section = {admit},
  title  = {Over free but under total is BLOCK — a wait, not a refusal},
  claim  = {Over free but under total is BLOCK — a wait, not a refusal.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_request_over_the_free_budget_blocks},
}

@misc{fence-admit-nesting-is-disjoint-so-an-inner-lease-counts-in-full,
  section = {admit},
  title  = {Outer 256 plus inner 256 holds 512, not 256 — a child draws from the global pool},
  claim  = {Outer 256 plus inner 256 holds 512, not 256 — a child draws from the global pool.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_nesting_is_disjoint_so_an_inner_lease_counts_in_full},
}

@misc{fence-admit-oversubscribed-tree-blocks-rather-than-deadlocking-or-refusing,
  section = {admit},
  title  = {TOTAL 512 with outer 384: an inner 256 under it BLOCKS},
  claim  = {TOTAL 512 with outer 384: an inner 256 under it BLOCKS.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_an_oversubscribed_tree_blocks_rather_than_deadlocking_or_refusing},
}

@misc{fence-admit-vanished-parent-is-refused,
  section = {admit},
  title  = {A parent no longer in the ledger refuses with the ledger code},
  claim  = {A parent no longer in the ledger refuses with the ledger code. THE CONTROL: the same
            request under a present parent is not refused for that.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_vanished_parent_is_refused},
}

@misc{fence-admit-ledger-with-no-total-is-refused-before-anything-else,
  section = {admit},
  title  = {No TOTAL_MB refuses with the ledger code, even for an impossible request},
  claim  = {No TOTAL_MB refuses with the ledger code, even for an impossible request.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_ledger_with_no_total_is_refused_before_anything_else},
}

@misc{fence-admit-claim-needs-no-capacity,
  section = {admit},
  title  = {`mb=0` with a claim is a first-class request, admitted with the budget exhausted},
  claim  = {`mb=0` with a claim is a first-class request, admitted with the budget exhausted.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_claim_needs_no_capacity},
}

@misc{fence-admit-held-claim-excludes-only-its-own-tag,
  section = {admit},
  title  = {`claim:x` held: another `claim:x` is CLAIMED, `claim:y` is admitted},
  claim  = {`claim:x` held: another `claim:x` is CLAIMED, `claim:y` is admitted.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_held_claim_excludes_only_its_own_tag},
}

@misc{fence-admit-gc-drops-a-dead-owner-and-keeps-a-live-one,
  section = {admit},
  title  = {A dead owner's lease goes; a live owner's stays},
  claim  = {A dead owner's lease goes; a live owner's stays.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_gc_drops_a_dead_owner_and_keeps_a_live_one},
}

@misc{fence-admit-gc-cascades-to-the-orphans-of-a-dead-parent,
  section = {admit},
  title  = {Parent P dead, child L alive under P, grandchild alive under L: all three go},
  claim  = {Parent P dead, child L alive under P, grandchild alive under L: all three go.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_gc_cascades_to_the_orphans_of_a_dead_parent},
}

@misc{fence-admit-gc-with-nothing-dead-returns-the-same-snapshot,
  section = {admit},
  title  = {Nothing dead is the SAME object — the caller's signal to skip the rewrite},
  claim  = {Nothing dead is the SAME object — the caller's signal to skip the rewrite. THE CONTROL:
            with one owner dead, a different snapshot comes back.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_gc_with_nothing_dead_returns_the_same_snapshot},
}

@misc{fence-admit-this-process-is-alive-by-its-own-identity,
  section = {admit},
  title  = {`owner_of(own pid)` reads as alive — the T-arm for the pid:starttime identity},
  claim  = {`owner_of(own pid)` reads as alive — the T-arm for the pid:starttime identity.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_this_process_is_alive_by_its_own_identity},
}

@misc{fence-admit-live-pid-with-the-wrong-starttime-is-dead,
  section = {admit},
  title  = {PID reuse: this pid with a start time it never had counts as dead},
  claim  = {PID reuse: this pid with a start time it never had counts as dead.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_live_pid_with_the_wrong_starttime_is_dead},
}

@misc{fence-admit-comm-with-spaces-and-parens-does-not-shift-the-fields,
  section = {admit},
  title  = {Fields are counted from the LAST `)`, so a comm like `a) b (c` reads field 22 correctly},
  claim  = {Fields are counted from the LAST `)`, so a comm like `a) b (c` reads field 22 correctly.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_comm_with_spaces_and_parens_does_not_shift_the_fields},
}

@misc{fence-admit-path-claims-compare-by-realpath,
  section = {admit},
  title  = {`claim:path:/a/b` and `claim:path:/a/b/` are one claim},
  claim  = {`claim:path:/a/b` and `claim:path:/a/b/` are one claim.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_path_claims_compare_by_realpath},
}

@misc{fence-admit-label-claims-are-never-realpathed,
  section = {admit},
  title  = {`claim:label://x:y` stays a label; `//x` and `//x:x` are one target},
  claim  = {`claim:label://x:y` stays a label; `//x` and `//x:x` are one target. THE CONTROL: a
            different target in the same package is a different claim.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_label_claims_are_never_realpathed},
}

@misc{fence-admit-bare-claims-stay-byte-compared,
  section = {admit},
  title  = {Bare `claim:a/b` and `claim:a/b/` BOTH acquire — the documented uncomparable fallback},
  claim  = {Bare `claim:a/b` and `claim:a/b/` BOTH acquire — the documented uncomparable fallback.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_bare_claims_stay_byte_compared},
}

@misc{fence-admit-label-without-the-claim-prefix-claims-nothing,
  section = {admit},
  title  = {An ordinary label is not a claim},
  claim  = {An ordinary label is not a claim.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_label_without_the_claim_prefix_claims_nothing},
}

@misc{fence-admit-load-gate-needs-both-conjuncts,
  section = {admit},
  title  = {Proceed only if load1 AND min(load5, load15) are under nproc × maxload},
  claim  = {Proceed only if load1 AND min(load5, load15) are under nproc × maxload.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_the_load_gate_needs_both_conjuncts},
}

@misc{fence-admit-zero-maxload-disables-the-load-gate,
  section = {admit},
  title  = {`maxload=0` admits under any load},
  claim  = {`maxload=0` admits under any load.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_zero_maxload_disables_the_load_gate},
}

@misc{fence-admit-init-declares-the-total-once,
  section = {admit},
  title  = {The first init writes TOTAL_MB; a second does not resize the shared pool},
  claim  = {The first init writes TOTAL_MB; a second does not resize the shared pool.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_init_declares_the_total_once},
}

@misc{fence-admit-default-total-is-seventy-percent-capped-at-eight-gib,
  section = {admit},
  title  = {`min(70% MemTotal, 8192)`: a small host gets 70%, a large one the cap},
  claim  = {`min(70% MemTotal, 8192)`: a small host gets 70%, a large one the cap.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_the_default_total_is_seventy_percent_capped_at_eight_gib},
}

@misc{fence-admit-lease-is-released-when-its-block-exits,
  section = {admit},
  title  = {Inside `admit` the lease is in the ledger; after it, it is gone},
  claim  = {Inside `admit` the lease is in the ledger; after it, it is gone.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_lease_is_released_when_its_block_exits},
}

@misc{fence-admit-impossible-request-is-refused-without-waiting,
  section = {admit},
  title  = {`mb > TOTAL_MB` raises at once, with no NOBLOCK and no sleep},
  claim  = {`mb > TOTAL_MB` raises at once, with no NOBLOCK and no sleep.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_an_impossible_request_is_refused_without_waiting},
}

@misc{fence-admit-noblock-refuses-a-request-that-would-wait,
  section = {admit},
  title  = {Held TOTAL-10: a second request for 20 with NOBLOCK exits 3 at once},
  claim  = {Held TOTAL-10: a second request for 20 with NOBLOCK exits 3 at once. THE CONTROL: the
            same request for 10 is admitted beside the holder.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_noblock_refuses_a_request_that_would_wait},
}

@misc{fence-admit-timeout-gives-up-with-the-refused-code,
  section = {admit},
  title  = {TIMEOUT: a blocked request gives up with 3 soon after its bound, not later},
  claim  = {TIMEOUT: a blocked request gives up with 3 soon after its bound, not later.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_timeout_gives_up_with_the_refused_code},
}

@misc{fence-admit-blocked-request-proceeds-after-the-holder-releases,
  section = {admit},
  title  = {A holds TOTAL-10, B asks 20 and BLOCKS; A releases during B's wait; B is admitted},
  claim  = {A holds TOTAL-10, B asks 20 and BLOCKS; A releases during B's wait; B is admitted. THE
            WAIT IS ASSERTED, not assumed: B's injected sleep is where A releases, so B
            can only be admitted after sleeping at least once.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_blocked_request_proceeds_after_the_holder_releases},
}

@misc{fence-admit-high-load-holds-a-start-back-and-zero-maxload-lets-it-through,
  section = {admit},
  title  = {Load over the ceiling with NOBLOCK is 3; the same load with `maxload=0` is admitted},
  claim  = {Load over the ceiling with NOBLOCK is 3; the same load with `maxload=0` is admitted.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_high_load_holds_a_start_back_and_zero_maxload_lets_it_through},
}

@misc{fence-admit-two-racing-claims-admit-exactly-one,
  section = {admit},
  title  = {Two processes claim `claim:x` at once: one holds (0), the other is refused (3)},
  claim  = {Two processes claim `claim:x` at once: one holds (0), the other is refused (3). THE RE-
            CHECK UNDER THE LOCK IS WHAT THIS PINS — both may pass the lock-free read.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_two_racing_claims_admit_exactly_one},
}

@misc{fence-admit-killed-holder-is-reaped-before-it-is-believed,
  section = {admit},
  title  = {A SIGKILLed holder's lease is reaped by the next contender, not waited on},
  claim  = {Holder H leases 400 of 512 and is SIGKILLed; its lease is STILL in the ledger; contender
            C asks 400 without waiting, reaps H, and is admitted. THE LEDGER IS READ RAW
            BETWEEN THE KILL AND THE CONTENDER — a reading that gc'd would remove the
            lease itself and C would never reach the reap branch this arm exists to
            test.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_killed_holder_is_reaped_before_it_is_believed},
}

@misc{fence-admit-reap-with-nothing-dead-leaves-the-file-untouched,
  section = {admit},
  title  = {Nothing dead: no rewrite, so the inode is unchanged. ⚑ THE CONTROL: a dead lease DOES},
  claim  = {Nothing dead: no rewrite, so the inode is unchanged. THE CONTROL: a dead lease DOES.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_reap_with_nothing_dead_leaves_the_file_untouched},
}

@misc{fence-admit-lock-is-acquired-within-its-bound-or-refused,
  section = {admit},
  title  = {A held lock makes a second acquire raise after its bound, not wait forever},
  claim  = {A held lock makes a second acquire raise after its bound, not wait forever.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_the_lock_is_acquired_within_its_bound_or_refused},
}

@misc{fence-ledger-bucket-is-the-next-power-of-two-above-a-floor,
  section = {ledger},
  title  = {65→128, 128→128, 129→256, 200→256, 257→512, 3→64 — never below the peak, never under 64},
  claim  = {65→128, 128→128, 129→256, 200→256, 257→512, 3→64 — never below the peak, never under 64.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_the_bucket_is_the_next_power_of_two_above_a_floor},
}

@misc{fence-ledger-row-round-trips-through-its-line,
  section = {ledger},
  title  = {Rendering then parsing yields the same row, with all seven fields},
  claim  = {Rendering then parsing yields the same row, with all seven fields.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_row_round_trips_through_its_line},
}

@misc{fence-ledger-absent-value-is-an-empty-field-not-zero,
  section = {ledger},
  title  = {A run with no probe writes empty peak and CPU fields, and reads them back as None},
  claim  = {A run with no probe writes empty peak and CPU fields, and reads them back as None. THE
            CONTROL: a measured zero stays a zero.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_an_absent_value_is_an_empty_field_not_zero},
}

@misc{fence-ledger-truncated-row-is-skipped-and-the-rest-survive,
  section = {ledger},
  title  = {A partial last line — a concurrent append — is skipped; the whole row before it is kept},
  claim  = {A partial last line — a concurrent append — is skipped; the whole row before it is kept.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_truncated_row_is_skipped_and_the_rest_survive},
}

@misc{fence-ledger-older-four-field-row-is-still-read,
  section = {ledger},
  title  = {A row from before the CPU probe (four fields) reads, with no CPU},
  claim  = {A row from before the CPU probe (four fields) reads, with no CPU.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_an_older_four_field_row_is_still_read},
}

@misc{fence-ledger-report-selects-by-label-prefix,
  section = {ledger},
  title  = {`selftest:` includes `selftest:a` and excludes `gate:a`},
  claim  = {`selftest:` includes `selftest:a` and excludes `gate:a`.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_the_report_selects_by_label_prefix},
}

@misc{fence-ledger-median-and-p90-follow-the-origins-index-rule,
  section = {ledger},
  title  = {Over (40, 188, 90): median 90, p90 188 — the origin's rule, not an interpolated one},
  claim  = {Over (40, 188, 90): median 90, p90 188 — the origin's rule, not an interpolated one.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_median_and_p90_follow_the_origins_index_rule},
}

@misc{fence-ledger-one-observation-has-a-p90,
  section = {ledger},
  title  = {A single run's p90 is that run — the index rule does not run off the end},
  claim  = {A single run's p90 is that run — the index rule does not run off the end.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_one_observation_has_a_p90},
}

@misc{fence-ledger-cpu-percent-is-unmeasured-when-no-row-carries-cpu,
  section = {ledger},
  title  = {Rows with no CPU give None, not 0. ⚑ THE CONTROL: rows that carry it give a number},
  claim  = {Rows with no CPU give None, not 0. THE CONTROL: rows that carry it give a number.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_cpu_percent_is_unmeasured_when_no_row_carries_cpu},
}

@misc{fence-ledger-suggested-size-is-the-leases-bucket-of-the-max,
  section = {ledger},
  title  = {`suggested` is exactly `bucket(max)` — one function, so column and lease cannot disagree},
  claim  = {`suggested` is exactly `bucket(max)` — one function, so column and lease cannot
            disagree.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_the_suggested_size_is_the_leases_bucket_of_the_max},
}

@misc{fence-ledger-label-with-no-peak-suggests-nothing,
  section = {ledger},
  title  = {Only probe-less rows: no max and no suggestion, rather than a floor-sized guess},
  claim  = {Only probe-less rows: no max and no suggestion, rather than a floor-sized guess.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_label_with_no_peak_suggests_nothing},
}

@misc{fence-ledger-clean-run-appends-exactly-one-seven-field-row,
  section = {ledger},
  title  = {Exit 0 appends one row: maxRSS as peak_mb, the cgroup peak as cg_peak_mb, the run's wall},
  claim  = {Exit 0 appends one row: maxRSS as peak_mb, the cgroup peak as cg_peak_mb, the run's
            wall.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_clean_run_appends_exactly_one_seven_field_row},
}

@misc{fence-ledger-failed-or-killed-run-appends-nothing,
  section = {ledger},
  title  = {rc!=0 — a failure, an OOM kill (137), a run never reaped — writes no row},
  claim  = {rc!=0 — a failure, an OOM kill (137), a run never reaped — writes no row. THE CONTROL IS
            THE ARM ABOVE: the same call with exit 0 writes one.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_failed_or_killed_run_appends_nothing},
}

@misc{fence-ledger-run-with-no-rusage-records-an-empty-peak,
  section = {ledger},
  title  = {No probe value → an empty peak field, not 0},
  claim  = {No probe value → an empty peak field, not 0.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_run_with_no_rusage_records_an_empty_peak},
}

@misc{fence-ledger-unwritable-ledger-is-not-an-error,
  section = {ledger},
  title  = {A read-only ledger directory returns False and raises nothing — the run still happened},
  claim  = {A read-only ledger directory returns False and raises nothing — the run still happened.
            THE CONTROL: the same record into a writable directory returns True.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_an_unwritable_ledger_is_not_an_error},
}

@misc{fence-ledger-fenced-run-carries-its-own-rusage,
  section = {ledger},
  title  = {A real fenced run reports its payload's maxRSS and CPU from `wait4`, not None},
  claim  = {A real fenced run reports its payload's maxRSS and CPU from `wait4`, not None.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_fenced_run_carries_its_own_rusage},
}

@misc{fence-ledger-fence-name-taken-in-the-same-second-does-not-collide,
  section = {ledger},
  title  = {A cgroup already named `.mikemol-fence.<pid>.<second>` does not stop the next run},
  claim  = {A cgroup already named `.mikemol-fence.<pid>.<second>` does not stop the next run. THE
            GATE'S COLLISION, MADE DETERMINISTIC: two Bazel sandboxes each saw their
            test process as pid 12 and fenced in the same second. Here the second is
            pinned and that name pre-created.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_fence_name_taken_in_the_same_second_does_not_collide},
}

@misc{fence-ledger-fence-name-carries-its-test-target,
  section = {ledger},
  title  = {Under Bazel the name ends in the test target, made safe for a directory name},
  claim  = {Under Bazel the name ends in the test target, made safe for a directory name.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_fence_name_carries_its_test_target},
}

@misc{fence-ledger-fence-name-outside-bazel-is-pid-and-second,
  section = {ledger},
  title  = {With no test target the name is `pid.second`, as it always was — the stated bound},
  claim  = {With no test target the name is `pid.second`, as it always was — the stated bound.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_fence_name_outside_bazel_is_pid_and_second},
}

@misc{fence-peaks-report-prints-each-label-and-states-its-method,
  section = {peaks},
  title  = {A label's line carries runs, max, median, p90 and the suggested bucket; the method follows},
  claim  = {A label's line carries runs, max, median, p90 and the suggested bucket; the method
            follows.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_peaks.py -k test_the_report_prints_each_label_and_states_its_method},
}

@misc{fence-peaks-unmeasured-cpu-prints-a-dash-not-zero,
  section = {peaks},
  title  = {Rows with no CPU print `-` in the cpu% column — never 0 for unmeasured},
  claim  = {Rows with no CPU print `-` in the cpu% column — never 0 for unmeasured.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_peaks.py -k test_unmeasured_cpu_prints_a_dash_not_zero},
}

@misc{fence-peaks-prefix-limits-the-report,
  section = {peaks},
  title  = {`selftest:` reports `selftest:a` and leaves out `gate:a`},
  claim  = {`selftest:` reports `selftest:a` and leaves out `gate:a`.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_peaks.py -k test_the_prefix_limits_the_report},
}

@misc{fence-peaks-missing-ledger-exits-one-and-says-so,
  section = {peaks},
  title  = {No ledger file: exit 1 and `no label ledger yet` on stderr, nothing on stdout},
  claim  = {No ledger file: exit 1 and `no label ledger yet` on stderr, nothing on stdout.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_peaks.py -k test_a_missing_ledger_exits_one_and_says_so},
}

@misc{fence-peaks-prefix-matching-nothing-exits-one,
  section = {peaks},
  title  = {Rows exist but none match: exit 1 with a message, rather than an empty table},
  claim  = {Rows exist but none match: exit 1 with a message, rather than an empty table.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_peaks.py -k test_a_prefix_matching_nothing_exits_one},
}

@misc{fence-peaks-no-ledger-argument-is-a-usage-error,
  section = {peaks},
  title  = {With no LEDGER the command refuses rather than guessing one},
  claim  = {With no LEDGER the command refuses rather than guessing one.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_peaks.py -k test_no_ledger_argument_is_a_usage_error},
}

@misc{fence-autosize-lease-is-the-bucket-of-the-max-not-the-median,
  section = {autosize},
  title  = {(40, 188, 90) leases 256: the bucket of 188, where the median's bucket would be 128},
  claim  = {(40, 188, 90) leases 256: the bucket of 188, where the median's bucket would be 128.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_the_lease_is_the_bucket_of_the_max_not_the_median},
}

@misc{fence-autosize-no-history-leases-the-default,
  section = {autosize},
  title  = {With no recorded peak the lease is the default, unclamped},
  claim  = {With no recorded peak the lease is the default, unclamped.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_no_history_leases_the_default},
}

@misc{fence-autosize-peak-over-the-cap-is-clamped-and-says-so,
  section = {autosize},
  title  = {Peak 600, ceiling 384: the lease is 384 and a warning names the key, peak and cap},
  claim  = {Peak 600, ceiling 384: the lease is 384 and a warning names the key, peak and cap. THE
            CONTROL: a peak under the cap yields no warning.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_a_peak_over_the_cap_is_clamped_and_says_so},
}

@misc{fence-autosize-explicit-default-above-the-ceiling-raises-the-cap,
  section = {autosize},
  title  = {DEFAULT=1024 with a 600 peak leases at least 600 — the origin's silent clamp to 384},
  claim  = {DEFAULT=1024 with a 600 peak leases at least 600 — the origin's silent clamp to 384.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_an_explicit_default_above_the_ceiling_raises_the_cap},
}

@misc{fence-autosize-climb-obeys-the-same-raised-cap,
  section = {autosize},
  title  = {DEFAULT=1024, a 384 lease killed: the climb may pass 384 — the origin's retry could not},
  claim  = {DEFAULT=1024, a 384 lease killed: the climb may pass 384 — the origin's retry could not.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_the_climb_obeys_the_same_raised_cap},
}

@misc{fence-autosize-climb-doubles-and-stops-at-the-cap,
  section = {autosize},
  title  = {64→128→256→384 under a 384 cap, then None: bounded, never a loop},
  claim  = {64→128→256→384 under a 384 cap, then None: bounded, never a loop.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_the_climb_doubles_and_stops_at_the_cap},
}

@misc{fence-autosize-only-a-memory-kill-triggers-a-climb,
  section = {autosize},
  title  = {`MEMORY, KILLED` climbs; a throttle, a pids trip and a bare SIGKILL do not},
  claim  = {`MEMORY, KILLED` climbs; a throttle, a pids trip and a bare SIGKILL do not. THE BARE 137
            IS THE ARM THE ORIGIN'S EXIT-CODE TRIGGER FAILS: `kill -9 $$` with no cap
            breach has an empty `bound_by` and must not climb.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_only_a_memory_kill_triggers_a_climb},
}

@misc{fence-autosize-climb-doubles-until-the-payload-fits,
  section = {autosize},
  title  = {A ~300MB payload from 64 climbs 64→128→256→384 and completes at 384},
  claim  = {A ~300MB payload from 64 climbs 64→128→256→384 and completes at 384.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_a_climb_doubles_until_the_payload_fits},
}

@misc{fence-autosize-without-retry-one-rung-runs-and-its-kill-is-the-result,
  section = {autosize},
  title  = {Retry off: one rung, killed by the cap, and nothing more},
  claim  = {Retry off: one rung, killed by the cap, and nothing more.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_without_retry_one_rung_runs_and_its_kill_is_the_result},
}

@misc{fence-autosize-payload-bigger-than-the-cap-stops-at-the-cap-killed,
  section = {autosize},
  title  = {A 600MB payload under a 384 cap climbs to 384, is killed there, and stops — bounded},
  claim  = {A 600MB payload under a 384 cap climbs to 384, is killed there, and stops — bounded.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_a_payload_bigger_than_the_cap_stops_at_the_cap_killed},
}

@misc{fence-autosize-self-inflicted-sigkill-does-not-climb,
  section = {autosize},
  title  = {A payload that `kill -9`s itself exits 137 with no cap breach, and the climb stops},
  claim  = {A payload that `kill -9`s itself exits 137 with no cap breach, and the climb stops. THE
            ARM THE ORIGIN'S EXIT-CODE TRIGGER FAILS; the control is the climbing arm
            above.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_a_self_inflicted_sigkill_does_not_climb},
}

@misc{fence-autosize-nested-climb-re-admits-under-the-outer-lease,
  section = {autosize},
  title  = {Inside an outer lease, every rung of an inner climb names the OUTER lease as its parent},
  claim  = {Inside an outer lease, every rung of an inner climb names the OUTER lease as its parent.
            NEVER THE LEASE IT JUST RELEASED — that retry would be a child of a corpse,
            and cascade-gc would remove it.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_a_nested_climb_re_admits_under_the_outer_lease},
}

@misc{fence-autosize-success-records-a-peak-the-next-size-is-taken-from,
  section = {autosize},
  title  = {After a climb succeeds, its recorded peak sizes the next lease — history converges},
  claim  = {After a climb succeeds, its recorded peak sizes the next lease — history converges.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_autosize.py -k test_a_success_records_a_peak_the_next_size_is_taken_from},
}

@misc{fence-admit-ledger-with-two-totals-is-refused-not-resolved,
  section = {admit},
  title  = {Two TOTAL_MB lines refuse with the ledger code — neither line is picked},
  claim  = {Two TOTAL_MB lines refuse with the ledger code — neither line is picked. THE CONTROL:
            the same request against one total is admitted.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_ledger_with_two_totals_is_refused_not_resolved},
}

@misc{fence-admit-malformed-second-total-still-counts,
  section = {admit},
  title  = {A second TOTAL_MB that does not parse is still a second claim about the pool's size},
  claim  = {A second TOTAL_MB that does not parse is still a second claim about the pool's size.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_malformed_second_total_still_counts},
}

@misc{fence-admit-ambiguous-ledger-is-never-rewritten,
  section = {admit},
  title  = {Gc over two totals and a dead lease leaves the file byte-for-byte — no silent repair},
  claim  = {Gc over two totals and a dead lease leaves the file byte-for-byte — no silent repair.
            THE CONTROL: the same dead lease under ONE total is reaped.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_an_ambiguous_ledger_is_never_rewritten},
}

@misc{fence-admit-init-refuses-an-ambiguous-ledger,
  section = {admit},
  title  = {Init over two totals raises with the ledger code rather than writing one — as bash does},
  claim  = {Init over two totals raises with the ledger code rather than writing one — as bash does.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_init_refuses_an_ambiguous_ledger},
}

@misc{fence-admit-acquire-refuses-an-ambiguous-ledger,
  section = {admit},
  title  = {A lease request against two totals raises at once with the ledger code — it never waits},
  claim  = {A lease request against two totals raises at once with the ledger code — it never waits.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_acquire_refuses_an_ambiguous_ledger},
}

@misc{fence-ledger-fractional-peak-is-written-as-whole-megabytes-rounded-up,
  section = {ledger},
  title  = {2049 KB writes as 3 MB, never 2.001 and never 2 — substrate's reader takes integers only},
  claim  = {2049 KB writes as 3 MB, never 2.001 and never 2 — substrate's reader takes integers
            only. THE CONTROL: an exact 2048 KB writes as 2, so the rounding is up, not
            a blanket +1.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_ledger.py -k test_a_fractional_peak_is_written_as_whole_megabytes_rounded_up},
}

@misc{fence-label-lease-largest-peak-drives-the-lease,
  section = {label_lease},
  title  = {(40, 188, 90) leases 256, and the reported peak is 188},
  claim  = {(40, 188, 90) leases 256, and the reported peak is 188.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_largest_peak_drives_the_lease},
}

@misc{fence-label-lease-rounding-goes-up-to-a-power-of-two-never-below-the-peak,
  section = {label_lease},
  title  = {Each of 65, 128, 129, 200, 257 leases a power of two at least the peak; 128 is exact},
  claim  = {Each of 65, 128, 129, 200, 257 leases a power of two at least the peak; 128 is exact.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_rounding_goes_up_to_a_power_of_two_never_below_the_peak},
}

@misc{fence-label-lease-small-peak-takes-the-floor,
  section = {label_lease},
  title  = {A 3 MB peak leases the 64 MB floor},
  claim  = {A 3 MB peak leases the 64 MB floor.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_small_peak_takes_the_floor},
}

@misc{fence-label-lease-light-label-sizes-below-the-default,
  section = {label_lease},
  title  = {(16, 15, 17) leases under the default of 192 — the concurrency point},
  claim  = {(16, 15, 17) leases under the default of 192 — the concurrency point.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_light_label_sizes_below_the_default},
}

@misc{fence-label-lease-unmeasured-label-takes-the-default,
  section = {label_lease},
  title  = {A sibling's rows do not size an unmeasured label; the control: they size the sibling},
  claim  = {A sibling's rows do not size an unmeasured label; the control: they size the sibling.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_an_unmeasured_label_takes_the_default},
}

@misc{fence-label-lease-missing-ledger-takes-the-default,
  section = {label_lease},
  title  = {A ledger that does not exist is no history, so the lease is the default},
  claim  = {A ledger that does not exist is no history, so the lease is the default.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_missing_ledger_takes_the_default},
}

@misc{fence-label-lease-another-labels-rows-are-not-read,
  section = {label_lease},
  title  = {A lone 16 beside a sibling's 900 leases the floor, not the sibling's size},
  claim  = {A lone 16 beside a sibling's 900 leases the floor, not the sibling's size.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_another_labels_rows_are_not_read},
}

@misc{fence-label-lease-peak-above-the-ceiling-clamps,
  section = {label_lease},
  title  = {A 900 peak under a 384 ceiling leases 384, clamped from 900},
  claim  = {A 900 peak under a 384 ceiling leases 384, clamped from 900.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_peak_above_the_ceiling_clamps},
}

@misc{fence-label-lease-clamp-message-names-the-override-and-says-below,
  section = {label_lease},
  title  = {A clamped lease's warning says BELOW and names `ceiling` (needs the autosize amendment)},
  claim  = {A clamped lease's warning says BELOW and names `ceiling` (needs the autosize amendment).},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_clamp_message_names_the_override_and_says_below},
}

@misc{fence-label-lease-unclamped-sizing-says-nothing,
  section = {label_lease},
  title  = {A lease under the cap owes no warning},
  claim  = {A lease under the cap owes no warning.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_an_unclamped_sizing_says_nothing},
}

@misc{fence-label-lease-explicit-default-raises-the-ceiling,
  section = {label_lease},
  title  = {Default 1024 with a 900 peak leases at least 900, unclamped},
  claim  = {Default 1024 with a 900 peak leases at least 900, unclamped.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_an_explicit_default_raises_the_ceiling},
}

@misc{fence-label-lease-malformed-rows-are-skipped,
  section = {label_lease},
  title  = {A truncated line and a non-numeric peak are skipped; the valid 188 leases 256},
  claim  = {A truncated line and a non-numeric peak are skipped; the valid 188 leases 256.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_malformed_rows_are_skipped},
}

@misc{fence-label-lease-fence-written-fractional-peak-is-read,
  section = {label_lease},
  title  = {A fence-written 187.53 peak leases 256 — the origin's integer reader saw no history},
  claim  = {A fence-written 187.53 peak leases 256 — the origin's integer reader saw no history.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_fence_written_fractional_peak_is_read},
}

@misc{fence-label-lease-slowest-run-drives-the-guard,
  section = {label_lease},
  title  = {(30, 511, 200) budgets 1080 — 2 * 511 up to the minute — and owes no warning},
  claim  = {(30, 511, 200) budgets 1080 — 2 * 511 up to the minute — and owes no warning.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_slowest_run_drives_the_guard},
}

@misc{fence-label-lease-quick-label-takes-the-floor,
  section = {label_lease},
  title  = {A 5 s run budgets the 120 s floor},
  claim  = {A 5 s run budgets the 120 s floor.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_quick_label_takes_the_floor},
}

@misc{fence-label-lease-untimed-label-takes-the-default,
  section = {label_lease},
  title  = {No history budgets the default, unclamped, with no slowest run},
  claim  = {No history budgets the default, unclamped, with no slowest run.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_an_untimed_label_takes_the_default},
}

@misc{fence-label-lease-guard-above-the-ceiling-clamps-loudly,
  section = {label_lease},
  title  = {A 3000 s run clamps to 3600, and the warning names the label and the risk},
  claim  = {A 3000 s run clamps to 3600, and the warning names the label and the risk.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_guard_above_the_ceiling_clamps_loudly},
}

@misc{fence-label-lease-slowest-cpu-drives-the-guard-when-it-outranks-wall,
  section = {label_lease},
  title  = {Wall 100 with CPU 300 + 100 budgets 1620 from 4 * 400, and reports cpu_s 400},
  claim  = {Wall 100 with CPU 300 + 100 budgets 1620 from 4 * 400, and reports cpu_s 400.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_slowest_cpu_drives_the_guard_when_it_outranks_wall},
}

@misc{fence-label-lease-row-without-cpu-is-not-read-as-zero,
  section = {label_lease},
  title  = {No CPU columns: cpu_s is None and the guard is the floor; the control carries CPU},
  claim  = {No CPU columns: cpu_s is None and the guard is the floor; the control carries CPU.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_a_row_without_cpu_is_not_read_as_zero},
}

@misc{fence-label-lease-clamp-message-reports-the-effective-cap,
  section = {label_lease},
  title  = {Under ceiling_s=300 a 1000 s run's warning names 300, not the module's 3600},
  claim  = {Under ceiling_s=300 a 1000 s run's warning names 300, not the module's 3600.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_clamp_message_reports_the_effective_cap},
}

@misc{fence-label-lease-one-half-of-cpu-is-no-cpu,
  section = {label_lease},
  title  = {A row with user but no sys (or sys but no user) counts as no CPU, not half of it},
  claim  = {A row with user but no sys (or sys but no user) counts as no CPU, not half of it.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_one_half_of_cpu_is_no_cpu},
}

@misc{fence-label-lease-cli-prints-the-number-and-warns-only-when-clamped,
  section = {label_lease},
  title  = {`lease` prints the MB on stdout; stderr is empty unclamped, and warns when clamped},
  claim  = {`lease` prints the MB on stdout; stderr is empty unclamped, and warns when clamped.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_cli_prints_the_number_and_warns_only_when_clamped},
}

@misc{fence-label-lease-cli-deadline-mode-prints-seconds,
  section = {label_lease},
  title  = {`deadline` prints the guard in seconds on stdout, with nothing on stderr},
  claim  = {`deadline` prints the guard in seconds on stdout, with nothing on stderr.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_cli_deadline_mode_prints_seconds},
}

@misc{fence-label-lease-cli-refuses-a-bad-invocation,
  section = {label_lease},
  title  = {An unknown mode, a short argv and a non-integer size each exit 2 with usage on stderr},
  claim  = {An unknown mode, a short argv and a non-integer size each exit 2 with usage on stderr.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_label_lease.py -k test_the_cli_refuses_a_bad_invocation},
}

@misc{fence-admit-reap-keeps-comment-and-unknown-lines,
  section = {admit},
  title  = {A gc rewrite keeps bash's `#` header and an unknown line kind verbatim (W1)},
  claim  = {A gc rewrite keeps bash's `#` header and an unknown line kind verbatim (W1). THE
            CONTROL: the dead lease in the same file IS dropped, so a rewrite really
            happened.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_reap_keeps_comment_and_unknown_lines},
}

@misc{fence-admit-release-keeps-comment-and-unknown-lines,
  section = {admit},
  title  = {Releasing a lease rewrites the file without losing a line of another kind (W1)},
  claim  = {Releasing a lease rewrites the file without losing a line of another kind (W1). THE
            CONTROL: the released lease is gone from the same file.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_release_keeps_comment_and_unknown_lines},
}

@misc{fence-admit-label-with-whitespace-is-refused,
  section = {admit},
  title  = {A label bash cannot hold as one word, a newline above all, is refused at construction (W2)},
  claim  = {A label bash cannot hold as one word, a newline above all, is refused at construction
            (W2). THE CONTROL: a one-word claim label constructs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_label_with_whitespace_is_refused},
}

@misc{fence-admit-default-ledger-is-bashs,
  section = {admit},
  title  = {`Store()` is `$MEMBUDGET_FILE`, else `$HOME/.cache/membudget/budget.cotype` (E1)},
  claim  = {`Store()` is `$MEMBUDGET_FILE`, else `$HOME/.cache/membudget/budget.cotype` (E1).},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_the_default_ledger_is_bashs},
}

@misc{fence-admit-run-creates-an-absent-ledger-but-not-a-totalless-one,
  section = {admit},
  title  = {An absent ledger is created with the default total (E2); a total-less FILE still refuses},
  claim  = {An absent ledger is created with the default total (E2); a total-less FILE still
            refuses. THE CONTROL IS THE SECOND HALF: a file that exists without a total
            is bash's exit 4 too — creation is for absence only, never a silent `init`.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_run_creates_an_absent_ledger_but_not_a_totalless_one},
}

@misc{fence-admit-request-inherits-membudget-parent,
  section = {admit},
  title  = {A request's default parent is `$MEMBUDGET_PARENT` (E3); unset or empty is top-level},
  claim  = {A request's default parent is `$MEMBUDGET_PARENT` (E3); unset or empty is top-level.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_a_request_inherits_membudget_parent},
}

@misc{fence-admit-admit-exports-its-lease-as-membudget-parent,
  section = {admit},
  title  = {Inside the block `MEMBUDGET_PARENT` is the lease's id, so a child nests; after, it is unset},
  claim  = {Inside the block `MEMBUDGET_PARENT` is the lease's id, so a child nests; after, it is
            unset. THE CONTROL: before the block the variable is absent (the `top_level`
            fixture's promise).},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_admit.py -k test_admit_exports_its_lease_as_membudget_parent},
}

@misc{fence-membudget-cli-run-args-parse-size-label-and-command,
  section = {membudget_cli},
  title  = {`run 300 job -- a b` reads 300, `job` and the command `a b`},
  claim  = {`run 300 job -- a b` reads 300, `job` and the command `a b`.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_args_parse_size_label_and_command},
}

@misc{fence-membudget-cli-run-label-is-optional,
  section = {membudget_cli},
  title  = {`run 300 -- a` has an empty label; the control carries one},
  claim  = {`run 300 -- a` has an empty label; the control carries one.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_label_is_optional},
}

@misc{fence-membudget-cli-env-maps-onto-waiting-and-the-lock,
  section = {membudget_cli},
  title  = {NOBLOCK, TIMEOUT, MAXLOAD and LOCK_TIMEOUT land in Waiting and Store; unset is admit's},
  claim  = {NOBLOCK, TIMEOUT, MAXLOAD and LOCK_TIMEOUT land in Waiting and Store; unset is admit's.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_env_maps_onto_waiting_and_the_lock},
}

@misc{fence-membudget-cli-status-renders-as-bash-does,
  section = {membudget_cli},
  title  = {Totals then one line per lease, top or under its parent; free counts top-level only},
  claim  = {Totals then one line per lease, top or under its parent; free counts top-level only.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_status_renders_as_bash_does},
}

@misc{fence-membudget-cli-an-unknown-verb-exits-2,
  section = {membudget_cli},
  title  = {An unknown verb and no verb exit 2; the control, `status`, exits 0},
  claim  = {An unknown verb and no verb exit 2; the control, `status`, exits 0.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_an_unknown_verb_exits_2},
}

@misc{fence-membudget-cli-a-malformed-invocation-exits-2,
  section = {membudget_cli},
  title  = {No `--`, no command, a bad MB, a spaced label, or wrong operands exit 2 and write nothing},
  claim  = {No `--`, no command, a bad MB, a spaced label, or wrong operands exit 2 and write nothing.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_a_malformed_invocation_exits_2},
}

@misc{fence-membudget-cli-a-bad-env-number-exits-2,
  section = {membudget_cli},
  title  = {A non-numeric MEMBUDGET_TIMEOUT exits 2; the control, a numeric one, runs},
  claim  = {A non-numeric MEMBUDGET_TIMEOUT exits 2; the control, a numeric one, runs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_a_bad_env_number_exits_2},
}

@misc{fence-membudget-cli-init-creates-the-total,
  section = {membudget_cli},
  title  = {`init 1000` on no file writes TOTAL_MB 1000},
  claim  = {`init 1000` on no file writes TOTAL_MB 1000.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_init_creates_the_total},
}

@misc{fence-membudget-cli-init-without-a-size-takes-the-default,
  section = {membudget_cli},
  title  = {`init` with no MB declares the host's default total},
  claim  = {`init` with no MB declares the host's default total.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_init_without_a_size_takes_the_default},
}

@misc{fence-membudget-cli-init-never-overwrites-a-total,
  section = {membudget_cli},
  title  = {A second `init 2000` leaves 1000 and says so; the control: the first init wrote 1000},
  claim  = {A second `init 2000` leaves 1000 and says so; the control: the first init wrote 1000.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_init_never_overwrites_a_total},
}

@misc{fence-membudget-cli-reset-resizes-and-keeps-a-live-lease,
  section = {membudget_cli},
  title  = {`init --reset 2000` sets 2000 and keeps a live lease and bash's `#` header},
  claim  = {`init --reset 2000` sets 2000 and keeps a live lease and bash's `#` header.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_reset_resizes_and_keeps_a_live_lease},
}

@misc{fence-membudget-cli-reset-below-the-leased-warns,
  section = {membudget_cli},
  title  = {A reset under the leased total warns over-subscribed; the control, above it, does not},
  claim  = {A reset under the leased total warns over-subscribed; the control, above it, does not.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_reset_below_the_leased_warns},
}

@misc{fence-membudget-cli-reset-refuses-two-totals,
  section = {membudget_cli},
  title  = {A ledger with two TOTAL_MB lines exits 4 and is left byte-for-byte},
  claim  = {A ledger with two TOTAL_MB lines exits 4 and is left byte-for-byte.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_reset_refuses_two_totals},
}

@misc{fence-membudget-cli-status-reaps-a-dead-lease,
  section = {membudget_cli},
  title  = {A dead owner's lease is dropped; the control, a live owner's, is listed},
  claim  = {A dead owner's lease is dropped; the control, a live owner's, is listed.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_status_reaps_a_dead_lease},
}

@misc{fence-membudget-cli-run-holds-its-lease-while-the-command-runs,
  section = {membudget_cli},
  title  = {The child sees MEMBUDGET_PARENT naming a lease in the ledger; its code is returned},
  claim  = {The child sees MEMBUDGET_PARENT naming a lease in the ledger; its code is returned.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_holds_its_lease_while_the_command_runs},
}

@misc{fence-membudget-cli-run-releases-its-lease,
  section = {membudget_cli},
  title  = {After `run` the ledger holds no lease, and the run created it with the default total},
  claim  = {After `run` the ledger holds no lease, and the run created it with the default total.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_releases_its_lease},
}

@misc{fence-membudget-cli-run-reports-a-signal-as-128-plus-it,
  section = {membudget_cli},
  title  = {A child killed by SIGTERM exits 143, as a shell reports it},
  claim  = {A child killed by SIGTERM exits 143, as a shell reports it.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_reports_a_signal_as_128_plus_it},
}

@misc{fence-membudget-cli-run-of-a-missing-command-exits-127,
  section = {membudget_cli},
  title  = {A command that does not exist exits 127, and its lease is still released},
  claim  = {A command that does not exist exits 127, and its lease is still released.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_of_a_missing_command_exits_127},
}

@misc{fence-membudget-cli-noblock-over-free-exits-3,
  section = {membudget_cli},
  title  = {With 300 of 1000 held, NOBLOCK 800 exits 3 without running; the control, 400, runs},
  claim  = {With 300 of 1000 held, NOBLOCK 800 exits 3 without running; the control, 400, runs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_noblock_over_free_exits_3},
}

@misc{fence-membudget-cli-timeout-gives-up-with-3,
  section = {membudget_cli},
  title  = {A tiny MEMBUDGET_TIMEOUT over free gives up with 3; the control, a fit, runs},
  claim  = {A tiny MEMBUDGET_TIMEOUT over free gives up with 3; the control, a fit, runs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_timeout_gives_up_with_3},
}

@misc{fence-membudget-cli-impossible-exits-3-and-no-total-exits-4,
  section = {membudget_cli},
  title  = {Over the total is 3; a file with no total is 4; the control, within the total, is 0},
  claim  = {Over the total is 3; a file with no total is 4; the control, within the total, is 0.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_impossible_exits_3_and_no_total_exits_4},
}

@misc{fence-membudget-cli-a-busy-load-holds-run-back,
  section = {membudget_cli},
  title  = {Under a busy injected load NOBLOCK exits 3; the control, MAXLOAD=0, runs},
  claim  = {Under a busy injected load NOBLOCK exits 3; the control, MAXLOAD=0, runs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_a_busy_load_holds_run_back},
}

@misc{fence-membudget-cli-a-held-lock-exits-1,
  section = {membudget_cli},
  title  = {With the lock held past MEMBUDGET_LOCK_TIMEOUT, `status` exits 1; released, it exits 0},
  claim  = {With the lock held past MEMBUDGET_LOCK_TIMEOUT, `status` exits 1; released, it exits 0.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_a_held_lock_exits_1},
}

@misc{fence-membudget-cli-run-kills-a-payload-over-its-lease,
  section = {membudget_cli},
  title  = {A 256MB payload under a 64MB lease is killed by the cap: 137, and stderr says so},
  claim  = {A 256MB payload under a 64MB lease is killed by the cap: 137, and stderr says so.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_kills_a_payload_over_its_lease},
}

@misc{fence-membudget-cli-run-under-its-lease-completes,
  section = {membudget_cli},
  title  = {The positive control: a 16MB payload under a 256MB lease completes with 0, unkilled},
  claim  = {The positive control: a 16MB payload under a 256MB lease completes with 0, unkilled.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_under_its_lease_completes},
}

@misc{fence-membudget-cli-run-without-a-fence-refuses-3,
  section = {membudget_cli},
  title  = {With no cgroup `run` exits 3, never starts the command, and releases its lease},
  claim  = {With no cgroup `run` exits 3, never starts the command, and releases its lease.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_run_without_a_fence_refuses_3},
}

@misc{fence-membudget-cli-the-cap-is-the-lease-with-swap-forbidden,
  section = {membudget_cli},
  title  = {`run 64` fences at memory 64M and swap 0 — `autosize.rung_caps`, restated nowhere},
  claim  = {`run 64` fences at memory 64M and swap 0 — `autosize.rung_caps`, restated nowhere.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_the_cap_is_the_lease_with_swap_forbidden},
}

@misc{fence-membudget-cli-auto-sizes-from-a-seeded-ledger,
  section = {membudget_cli},
  title  = {Peaks (40, 188, 90) for `job` lease 256 — the bucket of the max, from the run ledger},
  claim  = {Peaks (40, 188, 90) for `job` lease 256 — the bucket of the max, from the run ledger.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_auto_sizes_from_a_seeded_ledger},
}

@misc{fence-membudget-cli-auto-with-no-history-takes-the-default,
  section = {membudget_cli},
  title  = {With no history `auto` leases bash's 192; the control, AGDA_MB_DEFAULT=100, leases 100},
  claim  = {With no history `auto` leases bash's 192; the control, AGDA_MB_DEFAULT=100, leases 100.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_auto_with_no_history_takes_the_default},
}

@misc{fence-membudget-cli-auto-passes-the-ceiling-and-it-clamps,
  section = {membudget_cli},
  title  = {A 600MB peak leases 384 under AGDA_MB_MAX=384 and warns; under 2048 it leases 1024},
  claim  = {A 600MB peak leases 384 under AGDA_MB_MAX=384 and warns; under 2048 it leases 1024.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_auto_passes_the_ceiling_and_it_clamps},
}

@misc{fence-membudget-cli-auto-rejects-a-malformed-ceiling,
  section = {membudget_cli},
  title  = {AGDA_MB_MAX=lots is a usage error, 2; the control, an unset one, runs},
  claim  = {AGDA_MB_MAX=lots is a usage error, 2; the control, an unset one, runs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_auto_rejects_a_malformed_ceiling},
}

@misc{fence-membudget-cli-a-clean-run-is-recorded-and-a-failed-one-is-not,
  section = {membudget_cli},
  title  = {Exit 0 appends one row under its label; exit 1 appends none; unlabelled records as `?`},
  claim  = {Exit 0 appends one row under its label; exit 1 appends none; unlabelled records as `?`.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_a_clean_run_is_recorded_and_a_failed_one_is_not},
}

@misc{fence-membudget-cli-the-run-ledger-honours-its-path-and-opt-out,
  section = {membudget_cli},
  title  = {MEMBUDGET_LABEL_LEDGER moves the ledger; MEMBUDGET_NOLABELLEDGER=1 writes none at all},
  claim  = {MEMBUDGET_LABEL_LEDGER moves the ledger; MEMBUDGET_NOLABELLEDGER=1 writes none at all.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_the_run_ledger_honours_its_path_and_opt_out},
}

@misc{fence-membudget-cli-lease-and-deadline-answer-through-label-lease,
  section = {membudget_cli},
  title  = {With no history `lease` prints the default MB and `deadline` the default seconds},
  claim  = {With no history `lease` prints the default MB and `deadline` the default seconds.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_lease_and_deadline_answer_through_label_lease},
}

@misc{fence-membudget-cli-cross-python-reads-bash-init,
  section = {membudget_cli},
  title  = {Bash `init 1000` reads as 1000 here; a python reset to 2000 is what bash's status prints},
  claim  = {Bash `init 1000` reads as 1000 here; a python reset to 2000 is what bash's status prints.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_cross_python_reads_bash_init},
}

@misc{fence-membudget-cli-cross-bash-honours-a-python-lease,
  section = {membudget_cli},
  title  = {Under a live python 300 of 1000, bash NOBLOCK 800 exits 3; the control, bash 400, runs},
  claim  = {Under a live python 300 of 1000, bash NOBLOCK 800 exits 3; the control, bash 400, runs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_cross_bash_honours_a_python_lease},
}

@misc{fence-membudget-cli-cross-python-honours-a-bash-lease,
  section = {membudget_cli},
  title  = {Under a live bash 300 of 1000, python NOBLOCK 800 exits 3; the control, 400, runs},
  claim  = {Under a live bash 300 of 1000, python NOBLOCK 800 exits 3; the control, 400, runs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_cross_python_honours_a_bash_lease},
}

@misc{fence-membudget-cli-cross-python-reset-keeps-a-bash-lease,
  section = {membudget_cli},
  title  = {A python `init --reset` under a live bash lease keeps that lease},
  claim  = {A python `init --reset` under a live bash lease keeps that lease.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_cross_python_reset_keeps_a_bash_lease},
}

@misc{fence-membudget-cli-cross-bash-init-keeps-a-python-lease,
  section = {membudget_cli},
  title  = {Bash `init 2000` over a python lease changes nothing; bash `--reset` resizes and keeps it},
  claim  = {Bash `init 2000` over a python lease changes nothing; bash `--reset` resizes and keeps it.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_membudget_cli.py -k test_cross_bash_init_keeps_a_python_lease},
}

@misc{fence-git-env-every-git-variable-is-dropped-and-nothing-else-is,
  section = {git_env},
  title  = {All `GIT_*` go (no allowlist); a name merely CONTAINING `GIT` is the kept control},
  claim  = {All `GIT_*` go (no allowlist); a name merely CONTAINING `GIT` is the kept control.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_git_env.py -k test_every_git_variable_is_dropped_and_nothing_else_is},
}

@misc{fence-git-env-base-mapping-is-not-modified,
  section = {git_env},
  title  = {A caller's mapping comes back untouched; the result is a new dict},
  claim  = {A caller's mapping comes back untouched; the result is a new dict.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_git_env.py -k test_the_base_mapping_is_not_modified},
}

@misc{fence-git-env-default-base-is-this-processs-environment,
  section = {git_env},
  title  = {With no base, `os.environ` is read — and its `GIT_*` still dropped},
  claim  = {With no base, `os.environ` is read — and its `GIT_*` still dropped.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_git_env.py -k test_the_default_base_is_this_processs_environment},
}

@misc{fence-git-env-unscrubbed-fixture-commits-into-the-decoy,
  section = {git_env},
  title  = {THE HAZARD, measured: under the hostile environment a fixture's commit lands in the decoy},
  claim  = {THE HAZARD, measured: under the hostile environment a fixture's commit lands in the
            decoy. Without this arm the scrubbed arm could pass because the hazard never
            bit at all.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_git_env.py -k test_an_unscrubbed_fixture_commits_into_the_decoy},
}

@misc{fence-git-env-scrubbed-fixture-leaves-the-decoy-empty,
  section = {git_env},
  title  = {THE FIX: the same commit run under `clean_env` leaves the decoy with no refs},
  claim  = {THE FIX: the same commit run under `clean_env` leaves the decoy with no refs.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_git_env.py -k test_a_scrubbed_fixture_leaves_the_decoy_empty},
}
