% warrants.bib — one claim PER TEST CASE in the corpus suite.
%
% The claim text is TRANSCRIBED from each test function's own docstring, whose flagged
% paragraphs already argue the defect the case prevents.  Where a docstring is a bare
% one-liner the claim is correspondingly plain; that thinness is a fact about the case
% rather than something invented over.
%
% Keying convention: corpus-<section>-<test name minus its `test_` prefix, hyphenated,
% with leading articles dropped>.  Each check runs that one test function.


@misc{corpus-corpus-root-has-no-default-anywhere,
  section = {corpus},
  title  = {No walker defaults its root, and the module holds no `ROOT` to default to},
  claim  = {No walker defaults its root, and the module holds no `ROOT` to default to. substrate's
            `ROOT = Path(__file__).absolute().parent.parent` is its repo root in place
            and the venv's lib directory once installed, so its 173 importers would have
            censused site-packages with no error. Here, a walker called without a root
            refuses at the call.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_corpus.py -k test_the_root_has_no_default_anywhere},
}

@misc{corpus-corpus-unresolvable-root-raises-naming-the-request,
  section = {corpus},
  title  = {A path neither the current directory nor the root has raises, naming what was asked},
  claim  = {A path neither the current directory nor the root has raises, naming what was asked.
            Silently returning the default corpus is the bug: a peer nearly published
            substrate's census as their own repo's.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_corpus.py -k test_an_unresolvable_root_raises_naming_the_request},
}

@misc{corpus-corpus-exclusion-matches-whole-components-never-substrings,
  section = {corpus},
  title  = {`rebuild/` and `buildtools/` stay in; `build/`, caches, venvs and bazel links are out},
  claim  = {`rebuild/` and `buildtools/` stay in; `build/`, caches, venvs and bazel links are out.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_corpus.py -k test_exclusion_matches_whole_components_never_substrings},
}

@misc{corpus-corpus-root-discovery-uses-the-walk-vocabulary-and-the-caller-skip,
  section = {corpus},
  title  = {Generated, hidden and caller-skipped top-level trees are not discovered as roots},
  claim  = {Generated, hidden and caller-skipped top-level trees are not discovered as roots. A
            generated tree that is a TOP-LEVEL entry becomes its own root, so the
            component check below it never sees its name. And `agda` is substrate's
            policy, passed as `skip`.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_corpus.py -k test_root_discovery_uses_the_walk_vocabulary_and_the_caller_skip},
}

@misc{corpus-corpus-walk-of-a-real-tree-is-not-empty,
  section = {corpus},
  title  = {Walking a tree with sources returns at least one file: the positive control},
  claim  = {Walking a tree with sources returns at least one file: the positive control. Every
            exclusion returns FEWER files, and an over-eager one returns zero, which
            reads as a clean tree. The empty list passes sortedness vacuously, so this
            arm keeps the next two honest.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_corpus.py -k test_the_walk_of_a_real_tree_is_not_empty},
}

@misc{corpus-corpus-population-is-returned-sorted,
  section = {corpus},
  title  = {`py_files` hands its population back in sorted order, so a caller can cite it},
  claim  = {`py_files` hands its population back in sorted order, so a caller can cite it.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_corpus.py -k test_the_population_is_returned_sorted},
}

@misc{corpus-corpus-walk-finds-real-sources-and-omits-generated-copies,
  section = {corpus},
  title  = {A named source file is in the population; generated and skipped copies are not},
  claim  = {A named source file is in the population; generated and skipped copies are not. A named
            member is what separates a walk from a list: non-emptiness says the walk
            returned something, membership says it returned the right something.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_corpus.py -k test_the_walk_finds_real_sources_and_omits_generated_copies},
}

@misc{corpus-tree-root-bare-argv-refuses-naming-the-flag,
  section = {tree_root},
  title  = {An argv without `--root` refuses, and the refusal names the flag to pass},
  claim  = {An argv without `--root` refuses, and the refusal names the flag to pass.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_a_bare_argv_refuses_naming_the_flag},
}

@misc{corpus-tree-root-adding-only-the-flag-flips-the-refusal,
  section = {tree_root},
  title  = {The same argv plus only `--root <dir>` succeeds: the flag is READ, not refused always},
  claim  = {The same argv plus only `--root <dir>` succeeds: the flag is READ, not refused always.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_adding_only_the_flag_flips_the_refusal},
}

@misc{corpus-tree-root-flag-with-no-operand-refuses,
  section = {tree_root},
  title  = {`--root` as the last token refuses rather than reading past the end},
  claim  = {`--root` as the last token refuses rather than reading past the end.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_a_flag_with_no_operand_refuses},
}

@misc{corpus-tree-root-flag-is-never-taken-as-the-operand,
  section = {tree_root},
  title  = {`--root --apply` refuses for a missing directory, not for `--apply` not existing},
  claim  = {`--root --apply` refuses for a missing directory, not for `--apply` not existing.
            Consuming the next token blindly would bind `--apply` as the tree: the right
            verdict by the wrong route, and the caller told the wrong argument was at
            fault.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_a_flag_is_never_taken_as_the_operand},
}

@misc{corpus-tree-root-missing-tree-refuses,
  section = {tree_root},
  title  = {`of` refuses a path that is not a directory},
  claim  = {`of` refuses a path that is not a directory.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_a_missing_tree_refuses},
}

@misc{corpus-tree-root-flag-and-operand-are-consumed-in-any-position,
  section = {tree_root},
  title  = {`--root <dir>` leaves argv whether it leads, sits in the middle, or trails},
  claim  = {`--root <dir>` leaves argv whether it leads, sits in the middle, or trails. Order-
            independent, because a caller's own flags are not this module's to order.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_the_flag_and_operand_are_consumed_in_any_position},
}

@misc{corpus-tree-root-label-trims-inside-and-returns-outside-whole,
  section = {tree_root},
  title  = {Inside paths are trimmed; outside, relative, and the tree itself are returned whole},
  claim  = {Inside paths are trimmed; outside, relative, and the tree itself are returned whole. The
            outside arm is the crash class: `Path.relative_to` raises there.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_label_trims_inside_and_returns_outside_whole},
}

@misc{corpus-tree-root-holds-in-both-directions-and-not-a-name-prefix,
  section = {tree_root},
  title  = {An inside path is held; an outside path and a sibling sharing a name prefix are not},
  claim  = {An inside path is held; an outside path and a sibling sharing a name prefix are not.
            `/repo` is a string prefix of `/repository`: a bare `startswith` would claim
            it.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_holds_in_both_directions_and_not_a_name_prefix},
}

@misc{corpus-tree-root-package-files-names-the-package-asked-for-not-a-tree,
  section = {tree_root},
  title  = {`package_files` returns the named package's directory, holding its modules, not a tree},
  claim  = {`package_files` returns the named package's directory, holding its modules, not a tree.
            substrate's took no argument and asked for its own package, so moved here
            unchanged it would have answered with THIS distribution's files: the
            `corpus.ROOT` defect in a new place.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_package_files_names_the_package_asked_for_not_a_tree},
}

@misc{corpus-tree-root-census-resolves-every-root-and-refuses-a-missing-one,
  section = {tree_root},
  title  = {Every named tree resolves; one missing tree refuses the census; none yields nothing},
  claim  = {Every named tree resolves; one missing tree refuses the census; none yields nothing.
            Refusing rather than skipping: a dropped root shrinks the population, not
            the denominator.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_census_resolves_every_root_and_refuses_a_missing_one},
}

@misc{corpus-tree-root-namespace-package-is-refused-not-turned-into-a-bogus-path,
  section = {tree_root},
  title  = {`package_files` refuses a namespace package rather than returning a path to nowhere},
  claim  = {`package_files` refuses a namespace package rather than returning a path to nowhere.
            Measured while porting: `files()` on a namespace package is a
            `MultiplexedPath` whose `str` is not a path, and `Path(str(...))` made it
            one silently. The package here is built in tmp_path with no `__init__.py`,
            so it is a namespace package by construction.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_tree_root.py -k test_a_namespace_package_is_refused_not_turned_into_a_bogus_path},
}

@misc{corpus-import-edges-every-module-scope-import-shape-is-an-eager-edge,
  section = {import_edges},
  title  = {Imports at module scope, including inside if/try/except/finally/for/with, are all seen},
  claim  = {Imports at module scope, including inside if/try/except/finally/for/with, are all seen.
            Those bodies DO execute on import; a walk that skipped them under-reports,
            the worse direction: a suite certified as not reaching a store that aborts
            when run.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_import_edges.py -k test_every_module_scope_import_shape_is_an_eager_edge},
}

@misc{corpus-import-edges-imports-inside-definitions-never-leak-into-module-level,
  section = {import_edges},
  title  = {An import inside a def or class is NOT a module-level edge},
  claim  = {An import inside a def or class is NOT a module-level edge. The arm that would have
            caught substrate's measured 54-of-99 false positive: a lazy import read as
            eager made a lexer and a text gate both "reach" a store.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_import_edges.py -k test_imports_inside_definitions_never_leak_into_module_level},
}

@misc{corpus-import-edges-deferred-imports-are-reported-separately,
  section = {import_edges},
  title  = {Imports inside definitions are returned by `deferred`, not merged into module level},
  claim  = {Imports inside definitions are returned by `deferred`, not merged into module level.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_import_edges.py -k test_deferred_imports_are_reported_separately},
}

@misc{corpus-import-edges-relative-import-is-not-an-edge,
  section = {import_edges},
  title  = {`from . import x` contributes no name: it names nothing an index can resolve},
  claim  = {`from . import x` contributes no name: it names nothing an index can resolve.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_import_edges.py -k test_a_relative_import_is_not_an_edge},
}

@misc{corpus-import-edges-unparseable-or-missing-file-yields-no-edges,
  section = {import_edges},
  title  = {A syntax error or a vanished file degrades to no edges rather than raising},
  claim  = {A syntax error or a vanished file degrades to no edges rather than raising. A census
            asks every discovered file; the failure surfaces when the file is RUN.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_import_edges.py -k test_an_unparseable_or_missing_file_yields_no_edges},
}

@misc{corpus-import-edges-known-local-import-is-seen,
  section = {import_edges},
  title  = {A file with a known local import has that edge: the positive control},
  claim  = {A file with a known local import has that edge: the positive control. Zero edges is what
            a broken reader returns AND what a stdlib-only module returns; naming a file
            whose imports are known separates the two.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_import_edges.py -k test_a_known_local_import_is_seen},
}

@misc{corpus-import-edges-dotted-reading-names-submodule-candidates,
  section = {import_edges},
  title  = {`from X import Y` yields `X` and the candidate `X.Y`; the top-level reading yields `X`},
  claim  = {`from X import Y` yields `X` and the candidate `X.Y`; the top-level reading yields `X`.
            Every file in a package imports the package, so only the dotted reading can
            tell which module an intra-package edge points at.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_import_edges.py -k test_the_dotted_reading_names_submodule_candidates},
}

@misc{corpus-suite-discovery-both-live-dispatch-idioms-are-admitted,
  section = {suite_discovery},
  title  = {A __main__ guard, a bare argv dispatch, and a flag-naming guard with no body are suites},
  claim  = {A __main__ guard, a bare argv dispatch, and a flag-naming guard with no body are suites.
            Requiring the flag alone or the body alone erases one of the other two: both
            measured live.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_discovery.py -k test_both_live_dispatch_idioms_are_admitted},
}

@misc{corpus-suite-discovery-neither-half-alone-is-a-suite,
  section = {suite_discovery},
  title  = {A body with no dispatch is not a suite, and neither is a plain CLI entry point},
  claim  = {A body with no dispatch is not a suite, and neither is a plain CLI entry point. The
            entry point alone admits every CLI in the tree; the body alone admits
            implementation halves that report SILENT while their cases pass under a
            parent.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_discovery.py -k test_neither_half_alone_is_a_suite},
}

@misc{corpus-suite-discovery-vendored-and-snapshot-subtrees-are-walked-past,
  section = {suite_discovery},
  title  = {Copies under .venv, .edit-snapshots and __pycache__ are not suites; the real one is},
  claim  = {Copies under .venv, .edit-snapshots and __pycache__ are not suites; the real one is.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_discovery.py -k test_vendored_and_snapshot_subtrees_are_walked_past},
}

@misc{corpus-suite-discovery-roots-are-discovered-not-frozen,
  section = {suite_discovery},
  title  = {A new directory is a root, a tracked suiteless one is too, and a dot-directory is not},
  claim  = {A new directory is a root, a tracked suiteless one is too, and a dot-directory is not.
            `agda` is admitted deliberately: the name roster that skipped it was deleted
            by ruling, and a test pinning it would have kept a deleted mechanism alive
            through its test.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_discovery.py -k test_roots_are_discovered_not_frozen},
}

@misc{corpus-suite-discovery-walk-finds-the-suites-a-tree-holds,
  section = {suite_discovery},
  title  = {A tree with known suites yields exactly those: the positive control},
  claim  = {A tree with known suites yields exactly those: the positive control. Zero suites is what
            a broken walk returns AND what a correct walk returns over an empty tree;
            only naming files that must be found separates them.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_discovery.py -k test_the_walk_finds_the_suites_a_tree_holds},
}

@misc{corpus-suite-discovery-git-ignored-tree-is-not-walked,
  section = {suite_discovery},
  title  = {A top-level tree git ignores is not a root: git's ignore is the one owner of exclusions},
  claim  = {A top-level tree git ignores is not a root: git's ignore is the one owner of exclusions.
            Measured in substrate: a walker that never asked git kept counting
            `build/lib/`'s stale copy of every package module, twice, after `build/` was
            already fixed in git's view.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_discovery.py -k test_a_git_ignored_tree_is_not_walked},
}

@misc{corpus-suite-pragmas-dependency-pragma-is-read-wherever-it-sits,
  section = {suite_pragmas},
  title  = {One or several comma-separated deps are read, trimmed, even below code},
  claim  = {One or several comma-separated deps are read, trimmed, even below code. The pragma is a
            comment anywhere in the file, not a header: a suite that grew one later
            should not have to move it.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_pragmas.py -k test_the_dependency_pragma_is_read_wherever_it_sits},
}

@misc{corpus-suite-pragmas-no-dependency-pragma-is-empty-not-an-error,
  section = {suite_pragmas},
  title  = {A suite that declares no dependency yields an empty tuple},
  claim  = {A suite that declares no dependency yields an empty tuple.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_pragmas.py -k test_no_dependency_pragma_is_empty_not_an_error},
}

@misc{corpus-suite-pragmas-admitted-tenant-becomes-its-flag,
  section = {suite_pragmas},
  title  = {`sandbox` and `live`, admitted, declare `--sandbox` and `--live`},
  claim  = {`sandbox` and `live`, admitted, declare `--sandbox` and `--live`.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_pragmas.py -k test_an_admitted_tenant_becomes_its_flag},
}

@misc{corpus-suite-pragmas-typo-or-unadmitted-tenant-declares-nothing,
  section = {suite_pragmas},
  title  = {A misspelled or unknown tenant, no pragma, or an env-var tenant all declare nothing},
  claim  = {A misspelled or unknown tenant, no pragma, or an env-var tenant all declare nothing. The
            load-bearing arm: a misspelled tenant must declare NOTHING, so the runner
            never invents a tenant from a typo and reads plausible rows from a store
            nobody chose.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_pragmas.py -k test_a_typo_or_unadmitted_tenant_declares_nothing},
}

@misc{corpus-suite-pragmas-admitted-tenants-are-the-callers,
  section = {suite_pragmas},
  title  = {Another caller's set is honoured, and substrate's names are not built in},
  claim  = {Another caller's set is honoured, and substrate's names are not built in. substrate's
            `sandbox`/`live` is its store policy; a shared reader that hard-coded it
            would refuse every other repo's tenants and accept substrate's everywhere.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_pragmas.py -k test_the_admitted_tenants_are_the_callers},
}

@misc{corpus-suite-pragmas-unreadable-file-declares-nothing,
  section = {suite_pragmas},
  title  = {A vanished file declares no deps and no tenant, rather than raising},
  claim  = {A vanished file declares no deps and no tenant, rather than raising. The runner asks
            every discovered file; the failure surfaces when the suite is RUN.},
  check  = {cmd:.venv/bin/python3 -m pytest tests/test_suite_pragmas.py -k test_an_unreadable_file_declares_nothing},
}
