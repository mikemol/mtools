# `AX-` — the apex of `remaining-work`

**Written against `CENSUS-remaining-work.md` rev 16**, 2026-09-06. Prefix `AX-`. Apex; not a
surveyed party; did not write `§Q`, file any leg, or take part in any `§V` correction.

**Evidence arm** (per census-kit `§4`): the eight filed legs and the run file, read whole. Seven at
`findings/remaining-work/*.md`; `SB-` at `substrate/catalog/census/remaining-work-substrate.md`. No
peer tree was surveyed independently; **every claim about a repo's internal state is that leg's
testimony**, carried per `§C`, not adjudicated.

**Normative arm:** `§C` — span-with-witnesses, then glue with no admission bar — plus `§V`'s sixteen
amendments, applied as binding rather than as context. ⚑ **Where the run file's own readings are
wrong I say so** (`AX-7`); the normative arm is audited, not adopted.

**Denominator of this document: 8 legs · 47 numbered leg items · 33 `§Q`-6 unenumerable rows.**
Command: the per-leg item enumeration in `AX-2`, hand-built from each leg's own IDs.

---

## `AX-1` Phase 1 — the span `A`, with a witness per row

⚑⚑⚑ **`A` IS NEARLY EMPTY, AND THAT IS THE FIRST FINDING RATHER THAN A FAILURE OF THE PASS.**
`§C` says *build `A`: what every leg holds*. **Zero items are held by all eight legs.** The largest
witnessed correspondence class has **four** members. `§C`'s own construction presumes an
intersection this fleet does not have — every party surveyed its own ledger, and eight ledgers over
eight disjoint trees intersect in **method**, never in **work items**.

**So `A` splits into two kinds, and only the first is what `§C` asked for:**

### `AX-1a` `A-ITEM` — the same work item in two or more ledgers (3 rows)

| # | item | leg entries | witness (per `§3`) | kind |
|---|---|---|---|---|
| `A-ITEM-1` | the `membudget-ledger` ratchet island | `MT-04` ¶`⚑ AND substrate INVERTED two items` · `SB-4` | **byte-identity of the quoted command**: both quote `git log --all … -- scripts/membudget-ledger` → empty. Plus `SB-4`'s explicit cross-reference: *"`mtools` carried two items as blocked-on-substrate and had never asked."* Plus `SB-A1` naming `mtools-2e` as the finder. | **INVERTED PAIR** — mtools held it as *blocked on substrate*; substrate held it as *unstaged, mine*. Same object, opposite ledgers. |
| `A-ITEM-2` | mtools' gate refusing peers' census commits | `MT-04` rows 1–2 · `GB-03` (6 refusals, `:144`/`:283` diagnosis) · `LS-05` row 4 (*"six census commits refused, six distinct mechanisms, none my content"*) · `PK-04` ¶2 (*"my dirt refused `linux-sources` and warned `gabion`"*) | **the same third-party prose quoted in both** — "six refusals, six distinct mechanisms, none of them its content" appears in `MT-04`, `GB-03`, `LS-05` and the run file `§X`. Plus `PK-04` supplies the *cause* (its own uncommitted files) that the other three report as an effect. | **FOUR-LEG CLASS**, and the only one. ⚑ Four parties, four vantages, **one event**. |
| `A-ITEM-3` | paperkit ↔ summit `concept-library` / `library/` rename | `PK-03` row 2 · `SM-§Q-3` (3 asks on paperkit) | **explicit cross-reference by one leg to the other**: `PK-03` — *"I told summit to hold the old path and switch when I say it landed"*; `SM-§Q-3` — *"3 [asks blocked on] `paperkit` … filed"*. | **DIVERGENT** — see `AX-4`. |

### `AX-1b` `A-METHOD` — the same *finding about method*, independently reached (8 rows)

⚑ These are not work items and `§C` does not ask for them. They are in `A` because they are the
only thing all eight legs actually share, and an apex that reported `A` as empty would be reporting
a true number over the wrong population — `§V` rev 15's amendment 2 applied to my own span.

| # | the shared structure | legs holding it | witness |
|---|---|---|---|
| `A-M-1` | **the ledger is AUTHORED and nothing detects it going stale** | `MT-02` · `LS-02` · `PK-02` · `GB-02` · `RP-03` · `CO-2` (partial: gate is strong but closure-only) | byte-identity of `AUTHORED` in `§Q`-2 position. Command: `grep -co 'AUTHORED' *.md` → CO 1 · GB 1 · LS 2 · MT 2 · PK 2 · RP 1; SM 0, SB 0. **6 of 8 AUTHORED; 2 of 8 (`SM-`, `SB-`) DERIVED.** |
| `A-M-2` | **the drift check is UNBUILDABLE at this input, not merely absent** | `MT-02` · `LS-02` · `SM-§Q-2` · `RP-04` | byte-identity: `UNBUILDABLE` / *"unbuildable at your input"*. Command: `grep -ci unbuildable` → LS 2 · MT 1 · RP 2 · SM 1. Plus `LS-02` and `SM-§Q-2` **both explicitly cite mtools' glob-never-read-the-warrant formulation** — the same third-party prose in both. |
| `A-M-3` | **a `grep -c` over a word was presented as a population and was wrong** | `RP-01` (20→8→5, "wrong by 4×") · `MT-01` (9→6, 36→0) · `PK-01` (73→49, 40→38) · `SM-§Q-1` (319 over a projection; `14 of 20`) · `GB-07` (2 vs 3 vs 5, sets not nested) | each quotes its own command and both numbers. **Five legs, five independent corpora, one shape.** |
| `A-M-4` | **an obligation the filer FELT, reported as a block nobody was waiting on** | `CO-4` (9) · `GB-04` (2) · `PK-04` (2) · `LS-04` row 3 · `SM-§Q-4` (73→42) | `§V` revs 9–12 record each party running the split on its own leg and quoting the result. |
| `A-M-5` | **a peer's ledger records as owed something this party already delivered** | `LS-03`/`LS-06`-3 (`◆12`, `gcalculus`) · `SB-4` (mtools' two items) · `SM-§Q-6`-1 (69 unplaced drafts invalidating peers' denominators) | three legs, three directions, each quoting the counterparty's own artifact. |
| `A-M-6` | **the party cannot enumerate the refusals/blocks it has caused** | `MT-06`-1 · `CO-6`-2 · `GB-06`-1 · `RP-08` · `SB-6` row 3 · `SM-§Q-6`-1 | `CO-6`-2 explicitly cites the dispatcher's twelve; `RP-08` explicitly cites `§X`. **Cross-reference witness in 2; independent statement in 4.** |
| `A-M-7` | **the instrument was characterised from its output, not its source** | `MT-06`-5 (4 caught) · `RP-T1` (`head -40` truncation, "seventh instance") · `GB-10` (`\| tail` swallowing `$?`, 3 instances) · `SB-A1` (`--all` reads `refs/stash`) · `PK-05` row 2 | each quotes the defective command verbatim. |
| `A-M-8` | **the ledger's own home / gate exists and is not pointed at the ledger** | `GB-02` (gates 25 things, ledger ungated) · `SB-2` (`worklist.py` exists, holds Agda symbols only, and is itself gate-blocked) · `RP-03` (`cite-check.py` covers evidence, not argument) · `CO-2` (gate enforces closure over *written* rows) | each names its own gate and its own gap. |

### `AX-1c` NON-IDENTIFICATIONS — near-misses I did NOT merge, and why

⚑ Required by `§C` and by `§3`'s over-glue failure. **Nine, and three of them are traps the run
file's own text would have led me into.**

| pair | why NOT identified |
|---|---|
| `CO-1`'s 118 `open` · `SM`'s 10 `open asks` · `LS-01`'s 10 buildable-or-open | **`CO-1` explicitly forbids it**: *"`open` here means ACTIONABILITY, not epistemic openness… `§C` phase 1 must not identify two parties' `open` counts without a witness that they mean the same thing."* No such witness exists. Three different partitions of three different universes. |
| `SM`'s `14 closed / 10 open / 0 UNAVAILABLE` · every other leg's two-state block count | **`SM` supplies the non-identification itself** as its contribution to phase 1: *"If another leg reports blocked items as a two-state count, that is a non-identification, not a matching row."* Seven legs report two-state. **Honoured.** |
| `MT-03`'s 3 operator-blocked · `CO-3`'s 57 operator-blocked · `LS-03`'s `⊘2` · `GB-01` items 4–7 | *"the operator"* is **not a witnessed common referent**. Four legs name it; no leg quotes a message establishing they mean one queue. Carried as four separate edges in `AX-3`. |
| `PK-01`'s `Ζ·venv·build` · `RP-07`'s `.venv`-as-build-artifact | both are venv migrations both blocked on `build-hermeticity`'s outcome. **No cross-reference, no shared quote, different repos.** Similar summaries only — `§3` says that does not count. |
| `LS-01`'s two-reader `mdstruct` divergence (97 vs 100) · `SB-2`'s `scratch/mdstruct.py` gate-block · `CO-3`'s `mdstruct.py --headers` `ModuleNotFoundError` | ⚑ **Three legs, one filename, and I merged none of them.** `LS` measures the *mtools* binary against *substrate's* script; `SB` reports substrate's copy blocked by mypy; `CO` reports substrate's copy unimportable from cassian's route. Same name, **three different objects and three different failures**. This is `§V` rev 16 clause 3 — the homonym — arriving in the span. |
| `GB-04`'s summit floor entries · `SM-§Q-6`-1's 69 drafts | `GB-04` names *one* specific duplicate pair; `SM` counts *files*. `SM` itself warns its figure *"counts files, not entries"*. Not the same population. |
| `MT-01`'s `hook_no_chaining` unwired · `GB-10`'s `GB-01a` no-chaining hook | ⚑ **Nearly merged and correctly not.** `MT` reports *mtools'* hook wired in no `settings.json`; `GB-01a` reports *gabion's* hook exit code, retracted. Different repos, and the gabion claim is withdrawn. |
| `PK-07`'s `mat260` nomination · `MT-07`'s `mat260` in summit's inbox · `SM`'s `mat260` filings | `PK` nominates it as a **downstream consumer of paperkit by absolute path**; `MT`/`SM` see it as **a name in an intake queue**. Same string, two unrelated relations. Both carried. |
| `RP-05` (blocked on the dispatcher's hold) · `CO-5` row 5 (declined to file, same hold) | **Opposite classifications of one condition** — `RP` filed it as a BLOCK and filed anyway; `CO` filed it as a DECLINE and waited. Carried in `AX-4` as a divergence, not merged. |

---

## `AX-2` Phase 2 — the glue. Every leg's items carried

**47 numbered items across 8 legs.** Nothing is dropped for being unshared. The three `A-ITEM` rows
above are the only merges; the other 44 are single-leg and carried verbatim by ID.

| leg | `§Q`-1 ledger (as that leg defines it) | `§Q`-3 blocks | `§Q`-4 as filed | `§Q`-5 declines | `§Q`-6 rows |
|---|---|---|---|---|---|
| `MT-` | 6 rows; **3 of 6 stale when re-measured** | 3, all operator | 5 rows, all learned from the blocked party | 5 | **5** |
| `LS-` | 25 live of 97 sections (3 ◆ · 5–6 ◇ · 2 ⟦⟧ · 2 ⊘ · 13 ▣) | 2 | 3 rows | 4 | **5** |
| `SM-` | 13 producer rows; honest own-work board-red = **2 of 4** | 10 asks (6 substrate · 3 paperkit · 1 mat230) | 4 known of 8 deferrals | 4 | **4** |
| `PK-` | 12 open symbols | 2, **both on paperkit itself** | 2 | 4 | **2** (+ the growth-rate finding) |
| `CO-` | 118 (114 `open` + 4 `recorded`) of 433 labels | 60 (57 operator · 1 substrate · 1 mtools · 1 a peer's freeze) | 8 rows + 9 unmeasured capabilities | 6 | **5** |
| `RP-` | 8 rows (25 of 55 citation blocks · 6 of 9 axes · 8 of 9 rpm axes · 5 reads · 36 residue *mentions*) | 3 (`RP-05` dispatcher · `RP-06` self · `RP-07` structural) | 0, **measured** | 3 + 1 reversal | **4**, one struck CLOSED |
| `GB-` | 8 items (1 blocked · 2 owed-conditional · 4 deferred · 1 in flight) | 1, on mtools, 6 refusals | 2 | 7 | **3** |
| `SB-` | 481 witnesses (130 behavioural · 109 unwitnessed · **238 vacuous**); 8 gates would refuse · 29 UNMEASURABLE; 519 of 1488 modules shippable | 1 shared cause, 4 named files | 1 (mtools', found only because asked) | 5 | **7** |

⚑ **This output is larger than the largest leg** (`RP-` at 466 lines): the glue carries 47 items where
no leg carries more than 12 named ones. Phase 2 ran.

### `AX-2a` The unenumerable population — `§Q`-6 glued

**33 rows across 8 legs, and they do not sum.** They are not comparable quantities; four legs
say so explicitly. What they share is a *shape*, and the shape partitions cleanly into three:

| class | rows | example | is an instrument buildable? |
|---|---|---|---|
| **UNMEASURED — instrument known, not built** | 14 | `LS-06`-2 (recomputing checks), `CO-6`-4 (9 capabilities, `env -i` probe exists), `RP-13` (witness-multiplicity column), `SB-6` union-of-three | **yes.** This is deferred work wearing `§Q`-6's clothes. |
| **UNMEASURABLE FROM THIS VANTAGE — needs another party's tree** | 12 | `MT-06`-1, `CO-6`-2, `GB-06`-1 (*"in principle"*), `RP-08`, `SB-6` row 3, `LS-06`-3, `SM-§Q-6`-1 | **no — and this census is the instrument.** See `AX-6`. |
| **UNMEASURABLE IN PRINCIPLE — the population has no schema** | 7 | `PK-06` (*defects the repo has and I have not met*), `CO-6`-1 (*work never written down*), `CO-6`-5 (*what my shape vocabulary misses*), `SB-6` (*how much of 238 `standing` is legitimately standing*), `MT-06`-3/4 | **no.** Residue. |

⚑⚑⚑ **THE SECOND CLASS IS 12 OF 33 AND IT IS THE ONE THIS RUN DISCHARGED.** Every row in it says
*I cannot see what I do to others.* `AX-3` is that population, computed once, across all eight
vantages. **It is the only thing the census produced that no leg could have produced.**

---

## `AX-3` The bipartite relation — `§Q`-3 and `§Q`-4 as one object, per `§V` rev 12

`§V` rev 12 binds me: *read `§Q`-3 and `§Q`-4 as one bipartite relation measured twice from opposite
ends, and treat every edge present in exactly one of them as a FINDING rather than a discrepancy.*
Done. Vertices are the 8 rostered parties plus 5 named non-rostered ones.

### `AX-3a` Edges present in BOTH directions (2 of 27) — the safe cell

| edge | `§Q`-3 end | `§Q`-4 end | note |
|---|---|---|---|
| `gabion` ← `mtools` | `GB-03`: 1 item, 6 refusals | `MT-04` row 2: *"6 more, same"* | ⚑ Both ends agree, **and both say the blocker already knew.** `GB-03`: *"Reported, and mtools knows."* |
| `linux-sources` ← `mtools` | `LS-05` row 4 (as a decline, not a `§Q`-3 row) | `MT-04` rows 1 and 3 | Agreed on the fact; **filed under different questions at each end.** |

⚑ **TWO. Out of twenty-seven distinct edges asserted anywhere in the eight legs.** Rev 3a's
prediction — *some blocked items are visible from both ends, and those are not the dangerous ones* —
is confirmed and the population is **2 of 27, 7%**.

### `AX-3b` Edges asserted at the `§Q`-4 end only (claimed blocks with no `§Q`-3 counterpart)

Applying `§V` rev 12's three-population split. **`LS`' discriminator applied as PARTIAL per rev 13** —
it separates BLOCK from TODO and is blind to direction, so I apply a **second, independent** test for
sign: *does the row's own text make the filer's action conditional on the counterparty acting?*

| filer | row | counterparty's `§Q`-3? | verdict | basis |
|---|---|---|---|---|
| `CO-` | 8 rows (`◆build-census-leg`, `A171`, `A165`, `A159`, `A210/211`, `◆file-rbe-…`, `◆jsonl-…`) | ⚑ **none of the 8** appears in `LS-`'s or `SM-`'s `§Q`-3 | **8 TODO** | rev 9, measured by `CO-` itself against 7 legs. Rows cite cassian's own registry prose — **filer's inference**, `LS` discriminator → TODO. |
| `CO-` | summit's 9 unmeasured capability asks | ⚑ **`SM-§Q-3` does NOT list cassian** — its 10 asks split 6 substrate / 3 paperkit / 1 mat230 | ⚑⚑ **DIVERGENCE, not a TODO** | `CO-4` says *"summit is holding its registry open on my answer"* — a claim about summit's state. `SM-`'s own `§Q`-3 contradicts it. **Both stand; see `AX-4`.** |
| `GB-` | 2 rows (withdrawn-corroboration; pinned-reader friction) | none | ⚑ **2 INVERTED** | Both conditional on **summit** acting (`GB-01` owner-of-next-action column reads `summit` for both). Sign test fires. `LS` discriminator alone would have read these as BLOCKS — rev 13's exact warning. |
| `PK-` | 1 (the run file's stale header) | ⚑ appears at 5 legs' `§Q`-3-adjacent prose (`RP-T1`, `CO-5`, `SM`, `LS`, `GB`) | ⚑ **REAL BLOCK, and the only unanticipated one** | See `AX-3d`. |
| `PK-` | 1 (3 files uncommitted in mtools' tree) | ✔ `LS-05` row 4, `GB-03`, `MT-04` all report the *effect* | **REAL BLOCK, discharged** | The one `§Q`-4 row in the census that a counterparty's `§Q`-3 independently corroborates without the filer having been told first. |
| `SM-` | 4 known deferrals + 69/42 unplaced drafts | ⚑ **no leg's `§Q`-3 names summit as its blocker** | **42 BLOCK · 27 TODO** (per `§V` rev 12's 42-of-73 split, re-based to 69 at `SM`'s own commit figure) | The 42 have named counterparties with ages; `gcalculus` alone holds 9, oldest 21.1d, **on no roster**. |
| `SB-` | 1 (mtools' two items) | ✔ `MT-04` ¶`⚑` | **REAL BLOCK, resolved by the asking** | `A-ITEM-1`. |
| `LS-` | 3 rows | rows 1–2 ✔ (`SM` and `MT` both held censuses on the b-h freeze); row 3 ✘ | **2 BLOCK · 1 TODO** | rev 11, measured by `LS-` against `gcalculus`'s letter. |
| `MT-` | 5 rows | ✔ all five | **5 BLOCK, all learned from the blocked party** | ⚑ `MT-04` is the **only** `§Q`-4 in the census that is 100% blocks — because every row was reported *to* mtools rather than found *by* it. |
| `RP-` | 0 | — | **0, measured** | `RP-08`: 11 filed legs, 4 citing rosettapkg, 0 naming it as blocker. |

**`§Q`-4 TOTALS, three populations:**

    BLOCK      (waiting party, someone else)   50   — 42 summit drafts · 5 MT · 2 LS · 1 SB · 2 PK (1 discharged)
    TODO       (no waiting party)              36   — 8 CO · 27 SM own drafts · 1 LS
    INVERTED   (waiting party IS the filer)     2   — GB-04 both rows
    DIVERGENT  (two legs contradict)            1   — CO- ↔ SM- on the 9 capabilities

    filed as blocks under §Q-4: 89.  Measured blocks: 50 of 89 (56%).

⚑⚑⚑ **AND THE HEADLINE IS 42 OF THE 50.** Strip summit's intake queue and the whole fleet's
`§Q`-4 reduces to **8 real blocks across 7 parties**. `§Q`-4, asked of eight parties, found eight
edges. **One party's unplaced-mail queue outweighs the other seven ledgers 5:1.**

### `AX-3c` Edges asserted at the `§Q`-3 end only (blocks whose blocker filed nothing)

⚑ **This is the column `§Q`-4's defect makes structurally invisible, and it is the larger one.**

| blocked party | blocked on | did the blocker file it under `§Q`-4? |
|---|---|---|
| `CO-` (57 rows) · `MT-` (3) · `LS-` (1) · `GB-` (4 deferred) | **the operator** | ⚑ **N/A — the operator is not a rostered party and filed no leg.** 65 of the census's ~90 `§Q`-3 rows point at a vertex with no `§Q`-4 end. |
| `SM-` (6 asks) | `substrate` | ✘ `SB-4` names only mtools. **`SB-6` row 3 states the gap**: *"What any peer is blocked on from substrate, beyond what a peer tells me… No instrument here enumerates it."* |
| `SM-` (3 asks) | `paperkit` | ⚑ **PARTIAL** — `PK-03` row 2 names the rename, `PK-04` does not carry the three asks. |
| `SM-` (1 ask) | `mat230` | ✘ **structurally unanswerable** — mat230 has no inbox. `SM` reports it as a distinct tier. |
| `CO-` (1) | `substrate` (`◆routed-struct-tools-cannot-run`, 13 bare-name imports) | ✘ **absent from `SB-`'s leg entirely.** Carried. |
| `CO-` (1) | `mtools` (`◆cross-repo-write-authority`, the Ⓒ/Ⓓ sequencing hold) | ⚑ **`MT-03` carries it pointed at the OPERATOR, not at cassian.** `§V` rev 9 resolves the third account to a **dead pointer** (`mtools-05` is not a session that exists). Carried as `AX-4`. |
| `RP-` (`RP-07`) | *"the `build-hermeticity` census"* | ⚑ **a block on a PROCESS, not a party.** `RP-05`'s own gloss: *"there is no party who could know they are blocking me, because no party is."* |
| `LS-` (`⊘3`) | `gcalculus` | ✘ **not on the roster.** |
| `PK-` (`Ζ·venv·build`, 52 files) | **paperkit itself** | ⚑ self-edge; `PK-03` says so. |

**`§Q`-3 TOTALS:** ~90 asserted blocked-items. **65 point at the operator** (a non-party). **8 point
at a rostered peer.** **7 point at a non-rostered party or a process.** **~10 are self-edges.**

⚑⚑⚑ **SO THE TWO QUESTIONS BARELY MEET.** `§Q`-3 asserts ~90 blocked items; `§Q`-4 asserts 89
blocking rows; **the edges present in both directions number 2.** The bipartite relation is not a
relation with two noisy measurements — it is **two nearly disjoint edge sets** measured over
different vertex populations. `§Q`-3's dominant vertex (the operator, 65) has no `§Q`-4 end by
construction; `§Q`-4's dominant vertex (summit's intake, 42) has no `§Q`-3 end because the waiting
parties do not know they are waiting.

### `AX-3d` ⚑⚑⚑ The one block nobody predicted, and five parties paid it

**The run file's own header.** `PK-04` filed it under `§Q`-4 — *"what I am blocking is a correct
ruling resting on two expired reasons"* — and it is the **only** `§Q`-4 row in the census whose
counterparties can be enumerated from the legs themselves:

| party | what the header cost, in that leg's own words |
|---|---|
| `RP-` | `RP-T1`: *"I held this leg out of the tree for several ticks on that sentence. That was my error."* Filed as `RP-05`, a `§Q`-3 block. |
| `CO-` | `CO-5` row 5: filing this leg, **HELD**. `CO-`'s header: *"Cassian did not [file]… The correction was the thing that needed correcting."* Filed **fifth**. |
| `SM-` | `§V` rev 5: held its leg untracked, *"treating one grant as sufficient to CREATE but not to COMMIT."* |
| `GB-`, `LS-`, `PK-` | filed past it; each verified the trigger against `HEAD` first — a cost paid in verification rather than delay. |

⚑ **Six of eight legs spent measurable work on a stop sign whose justification was refuted.** `PK-`
is the blocker of record and **could not clear it** — *"I co-authored that header and I cannot edit
it; `mtools` committed the merged version and owns the tree."* **A block whose owner has no write
access to the object.** That cell is in no leg's vocabulary and in no `§Q` question.

---

## `AX-4` Divergence register — both stand, with denominators

Per `§C`: where two legs disagree, both stand. **Four.**

| # | leg A | leg B | denominators | status |
|---|---|---|---|---|
| `AX-D1` | `CO-4`: *"summit is holding its registry open on my answer"* — 9 of 11 capability asks unmeasured | `SM-§Q-3`: 10 open asks, **0 on cassian** (6 substrate · 3 paperkit · 1 mat230) | A: 11 asks, cassian's count. B: 10 open of 24, `summit ask`, exit-code-derived | ⚑ **UNRESOLVED, and it is not symmetric.** `SM`'s figure is *computed* (an ask is open exactly when its check exits non-zero); `CO`'s is a *reading of what summit needs*. **But `SM-§Q-6`-2 independently says `0 of the 26 are summit's to upgrade`** — i.e. summit knows it is waiting on owners. Two instruments over two definitions of "ask". **Both carried.** |
| `AX-D2` | `CO-3`: 1 item blocked on `mtools` — the Ⓒ/Ⓓ sequencing hold | `MT-03`: *cassian's 3 hook components*, blocked on **the operator** | A: 118-row registry. B: 6-row authored ledger | ⚑⚑ **Three accounts, no two agreeing**, and `§V` rev 9 measured the third to a **dead pointer**: `git log --all -S 'hook_no_verify' -- .claude/` → nothing; `mtools-05` is not a session that exists. **The disagreement is real and neither party is wrong** — two questions were merged (*who adopts* vs *who releases*) and only one is mtools'. |
| `AX-D3` | `RP-05`: the hold is a **BLOCK** on the dispatcher; filed anyway | `CO-5` row 5: the hold is a **DECLINE**; waited | one condition, two classifications | ⚑ **Both defensible and the outcomes differed.** `CO-` calls its own choice the error: *"I took a peer's statement over my own measurement."* `RP-` calls its earlier waiting the error. **Same event, both parties self-critical, opposite directions.** Carried unadjudicated. |
| `AX-D4` | `PK-03`: *"Nothing is blocked on another party"* | `SM-§Q-3`: **3 asks blocked on paperkit** | A: 12 symbols. B: 24 asks | ⚑ **PK's negative is bounded to its own symbol list and SM's edge is real.** Not a contradiction of fact — **a contradiction of population.** `PK-01`'s denominator is *symbols paperkit has named for itself*, which `PK-06` says is not *work paperkit has*. **The leg predicted its own gap and the divergence confirms it.** |

---

## `AX-5` ⚑⚑⚑ Where the run file's own readings are wrong

The dispatcher asked for this explicitly. **Five, ordered by how load-bearing they are.**

### `AX-5a` `§C`'s construction presumes an intersection that does not exist

`§C`: *"Build `A`: **what every leg holds**."* **Zero items are held by all eight legs**, and the
largest work-item class has four members (`A-ITEM-2`). The construction as written computes an
empty span, and an apex following it literally would publish `A = ∅` and then have nothing to glue
along.

⚑ **The defect is that `A` was specified as an intersection over ITEMS when the survey's subject
guarantees disjointness.** Eight parties surveying *their own* ledgers, over eight trees, cannot
share work items except where one party's tree touches another's. **`A` is not a tuning knob here —
it is structurally near-empty, and `§3`'s under-glue/over-glue framing does not reach that case.**
What the legs actually share is *method*, which `§C` never asked for and which is 8 rows to
`A-ITEM`'s 3. **I published both and labelled which is which.**

### `AX-5b` `§X`'s "twelve peer-blocking events" is quoted by four legs as a measured fact and it is a lower bound with an unstated denominator

`§X`: *"Twelve peer-blocking events, from one repo's gate, none of which appeared in that repo's
ledger."* **`CO-6`-2 and `RP-08` both reason from it explicitly.** But `MT-06`-1 — the same party,
same session — states: *"The gate emits a refusal account on every failure and **nothing aggregates
them**… Every figure in `MT-04` is a lower bound with an unknown denominator."*

⚑⚑ **So the census's most-cited number is one the party that produced it disclaims in its own
leg**, and two peers built `§Q`-6 rows on it as though it were a count. Per `§V` rev 15, this is
the third row of the four-way table — **the input was never retained; there is no corpus.** The
figure is *testimony about what parties reported*, not a measurement of refusals. **Twelve is a
count of REPORTS RECEIVED, and its denominator is unmeasurable.**

### `AX-5c` `§V` rev 12's `0 of 7 frozen legs list gabion as blocking them` uses a positive control that fails clause 4

The control offered: *"the instrument found all 47 [gabion mentions] and the three block-adjacent
hits resolve to `MT-03` and `rosettapkg` citing gabion's methodology."*

⚑ That control proves **the reader can find the string `gabion`**. The claim is *no leg reports
being blocked by gabion*. Per `§V` rev 16 clause 4, the control must validate **the claim's**
question: it would need a positive case of *a leg naming a party as its blocker* — which exists and
was not used (`GB-03` names mtools; `SM-§Q-3` names substrate/paperkit/mat230). **The control is
claim-shaped for the reader's question (`can I find gabion`) and not for the claim's (`can I detect
a blocking relation`).** The conclusion is almost certainly right; the control does not establish
it. ⚑ *This is exactly the defect rev 16 clause 4 names, committed inside the row that publishes
clause 3.*

### `AX-5d` `§V` rev 10's `§S`-based `0 of 7` and rev 12's `0 across all six` measure over rosters that exclude the filer AND the one leg filed elsewhere

`PK`'s measurement: `grep -ciE "blocked on .{0,40}paperkit|waiting on .{0,40}paperkit"` → *0 across
all six.* **Six.** The roster is eight; `PK` is one, leaving seven. **`SB-` was filed elsewhere and
is not in `findings/remaining-work/`, so a directory-scoped grep cannot see it.**

⚑ **Positive control on my own claim** — the command `grep -l 'blocked on' findings/remaining-work/*.md` returns
`cassian-observability.md · linux-sources.md · mtools.md · paperkit.md · rosettapkg.md · summit.md`;
`remaining-work-substrate.md` is absent from that directory entirely, and holds `SB-3`/`SB-4` under
different headings. **The reader sees the shape in 6 files and cannot reach the 8th.** `§V` rev 6
created `filed elsewhere` as a *status*, and **no measurement in the run file was re-scoped after
it.** Every `n of 6` and `n of 7` in `§V` is over a population that silently excludes substrate.

⚑⚑ **This is the run file's own phantom-path defect (`§V` rev 3b) in its new form**: not a path that
never existed, but a **real path outside the glob**. The accounting converged; the *measurements*
did not.

### `AX-5e` The header's own claim that five parties were right is true, and the run file never counted the sixth and seventh who were also right and paid for it

Rev 5: *"FIVE PARTIES FILED PAST A HEADER READING `DO NOT FILE`, AND EVERY ONE OF THEM WAS RIGHT."*
⚑ Correct. **But `CO-` and `RP-` both measured the header as stale and did NOT file** — `CO-` says
so directly (*"Two ticks ago I read that header as stale — which was correct — and then deferred"*),
`RP-` says so directly (*"I held this leg out of the tree for several ticks on that sentence"*).
**Seven of eight parties reached the correct reading; five acted on it and two deferred to the
author.** The run file reports the 5 and not the 7, so it reads as *five careful readers* when the
measurement is *seven careful readers and a two-party compliance failure in the deferential
direction*. ⚑ **The stronger finding is the one the smaller number hides:** the header was not merely
mis-written, it was **correctly diagnosed by 7 of 8 and still cost 2 of them their filing order** —
because a peer's assertion outranked their own measurement. `AX-3d` is that cost, counted.

---

## `AX-6` The shape, which is the finding — `§C`'s deliverable

**Not a total.** Three proportions and one structural fact.

### `AX-6a` How much of the fleet's remaining work is blocked on another party

    §Q-3 asserted blocked items, all legs                    ~90
      pointing at THE OPERATOR (not a party to this census)   65   (72%)
      pointing at a ROSTERED PEER                              8   ( 9%)
      pointing at a NON-ROSTERED party or a PROCESS            7   ( 8%)
      self-edges (the filer blocks itself)                   ~10   (11%)

⚑⚑⚑ **THE FLEET IS NOT BLOCKED ON ITSELF. IT IS BLOCKED ON ITS OPERATOR, 8:1 OVER ALL PEER
EDGES COMBINED.** `§Q`-3 and `§Q`-4 were built to find the peer-to-peer edges nobody can see from
one vantage; there are **eight**, and 72% of the census's blocked work points at a vertex the
census never rostered, never asked, and structurally could not ask. **The question found the small
population and missed the large one by construction.**

### `AX-6b` How much is unenumerable

    §Q-6 rows, all legs                                       33
      UNMEASURED, instrument known and buildable              14   (42%)
      UNMEASURABLE from this vantage — needs a peer's tree    12   (36%)
      UNMEASURABLE in principle — no schema for the population  7   (21%)

⚑ **36% of the census's unenumerable work was discharged by running the census**, and that is the
run's measurable yield. `A-M-6` shows six legs independently naming *I cannot count what I do to
others*; `AX-3` computes it once across all eight. **42% is deferred work mislabelled as
unmeasurable** — an instrument is named in each of those 14 rows.

### `AX-6c` How much appears in two ledgers as different items

    distinct edges asserted anywhere                          27
      present at BOTH ends                                     2   ( 7%)
      §Q-4 end only (claimed block, no waiting party found)   14   (52%)
      §Q-3 end only (blocked, blocker filed nothing)          11   (41%)

    §Q-4 rows filed as blocks                                 89
      measured BLOCK                                          50   (56%)  — 42 of them one queue
      TODO                                                    36   (40%)
      INVERTED                                                 2   ( 2%)
      DIVERGENT                                                1   ( 1%)

⚑⚑⚑ **93% OF ASSERTED EDGES ARE ONE-ENDED.** Per `§V` rev 12 every such edge is a finding, so this
census's substantive output is **25 findings and 2 confirmations.** ⚑ *A relation measured from both
ends that agrees 7% of the time is not a noisy measurement of one relation. It is two different
relations sharing a name* — and the run file's instruction to read them as one bipartite object,
followed literally, is what makes that visible.

### `AX-6d` ⚑⚑⚑ The structural fact: `§Q`-1 and `§Q`-6 are the same question and the legs answer them inconsistently

Every leg's `§Q`-1 denominator is *a population that leg chose*. Every leg's `§Q`-6 is *the
population it could not choose*. **The ratio between them is not comparable across legs and four
legs say so in their own text** — `PK-01` (*"symbols paperkit has named for itself, which is not the
same as work paperkit has"*), `CO-1` (*"`open` means ACTIONABILITY"*), `SB-1` (*"not the denominator
of substrate's remaining work"*), `SM-§Q-1` (*"the durable content of this table is the PRODUCER
column"*).

⚑ **So the fleet's total remaining work is not merely unknown — it is not a quantity.** Summing
`6 + 25 + 13 + 12 + 118 + 8 + 8 + 481` produces 671 and the number is **INVALID**, per `§V` rev 15
amendment 1: a comparison that cannot be made reports INVALID, never a figure. The eight
denominators are *labels*, *sections*, *symbols*, *asks*, *rows*, *items*, *witnesses* and
*modules*. **They do not commensurate, and no leg claimed they would.**

⚑⚑ **AND `§V` REV 15 AMENDMENT 3 FORBIDS THE FALLBACK.** A checksum over a partition validates the
SUM and never the blocks — so even a defensible total would not license any reader to consume a
cell. **`§C` is right that the sum is not the answer; the sharper statement is that the sum does not
exist.**

---

## `AX-7` What `§Q` produced that is garbage, stated plainly

- **`§Q`-4 produced 39 spurious rows of 89 (44%).** Its defect is fully documented across `§V`
  revs 9–13 and I add nothing to the diagnosis — only the count. ⚑ **The repair is one clause**:
  *name the counterparty and quote the artifact in which they are waiting.* Every row that survives
  my split does exactly that; every row that does not, does not.
- **`§Q`-3 produced 65 rows pointing at a non-party.** Not garbage — but the census cannot use them,
  and `§Q`-3's framing (*"the blocking party frequently does not [know]"*) is **false for 72% of its
  own answers**: the operator is told every tick by four separate legs. ⚑ **`§Q`-3's premise holds
  for 8 of ~90 rows.**
- **`§Q`-1's demand for a denominator was answered honestly and is not aggregable** (`AX-6d`). The
  demand improved every leg and produced no fleet-level quantity. **That is a correct outcome of a
  question that expected otherwise.**
- **`§Q`-6 is the question that worked.** 33 rows, 3 clean classes, 12 discharged by this apex.
  ⚑ It is also the only `§Q` question with **no** recorded defect in `§V`'s sixteen revisions.
- **`§Q`-5 (declines) produced 38 rows and zero divergences.** ⚑ A decline with a stated reason is
  the one artifact in this census that no other party contradicted. **Not because it was checked —
  because nothing in `§C` cross-checks it.** Recorded as an unexamined population, not a clean one.

---

## `AX-8` Absence claims made by THIS document, with controls (`§5`, four clauses)

| claim | spelling searched | denominator | positive control | clause check |
|---|---|---|---|---|
| **Zero items are held by all eight legs** | hand correspondence over all 47 named items, both directions | 8 legs × their own IDs | ⚑ `A-ITEM-2` **is** a four-leg identification found by the same method with a quoted-prose witness — the method finds multi-leg items when they exist | 1 ✓ read · 2 ✓ shape is *an item in n legs* · 3 ✓ referent is leg items, not strings · 4 ✓ validates *does a shared item exist*, which is the claim |
| **No leg's `§Q`-3 names `cassian` as its blocker** | read all 8 `§Q`-3 sections in full | 8 legs | ⚑ `SM-§Q-3` names three parties (substrate/paperkit/mat230); `CO-3` names two (substrate/mtools) — **the reader sees a `§Q`-3 blocker-naming when one is present**, which is the claim's question, not `can I find the string cassian` | 1 ✓ · 2 ✓ · 3 ✓ · 4 ✓ — ⚑ **this is the control `§V` rev 12 should have used and did not** (`AX-5c`) |
| **`SB-` is absent from every `findings/remaining-work/` glob measurement in `§V`** | `grep -l 'blocked on' findings/remaining-work/*.md` → 6 files, substrate not among them | the 8-party roster | the same command **finds the shape in 6 of 7 files present**; substrate's leg holds the shape under different headings at another path | 1 ✓ · 2 ✓ · 3 ✓ · 4 ✓ |
| **The operator filed no leg and has no `§Q`-4 end** | `§S`, all 8 rows | `§R`'s 8 surveyors | ⚑ `§S` **does** record a filing state for all 8 rostered parties, including `filed elsewhere` for substrate — the roster's state vocabulary is functioning; the operator has no row **because `§R` never listed one** | 1 ✓ · 2 ✓ · 3 ✓ · 4 ✓ — the claim is about the ROSTER, not about the operator's conduct |

⚑ **A control I could not construct**, filed in the weak honest form per `§5`: I cannot exhibit a
control for *"no party outside these eight is blocked on a rostered party in a way no leg records."*
Four legs nominate non-rostered parties (`gcalculus`, `mat260`, `mat230`, `el-openglo`,
`memory-concepts`, `freecell`, `el-openglo`, `the next substrate session`) and **none of them
filed**. The honest statement is: *no such block appears among the eight vantages I can read.*

---

## `AX-9` Roster nominations, carried as first-class remainder (`§6`)

Nominated by legs, **none rostered, none reached**:

| nominee | by | what they hold | reachable? |
|---|---|---|---|
| `gcalculus` | `LS-03`/`LS-04`, `MT-07`, `SM` | ⚑ **9 letters in summit's intake, all stale, oldest 21.1d** (`§V` rev 12) · owed `◆12`'s report from `LS-` · a `⊘3` discoverability block | has an inbox; **on no roster in this fleet** |
| `mat260` | `PK-07`, `MT-07`, `SM` | consumes paperkit's engine by absolute path, **fails soft** (`else echo "paperkit absent … SKIPPED"`) | ⚑ **a blocked party that cannot signal** |
| `mat230` | `SM-§Q-3` | 1 open ask | ⚑ **structurally unreachable — no inbox to reply into** |
| `el-openglo` | `GB-`, `SB-N`, `MT-07` | b-h leg; `catalog/worklist/warrants.bib` | unmeasurable (`ListAgents` tristate) |
| `memory-concepts`, `freecell` | `GB-`, `MT-07` | b-h legs / intake filings | unmeasurable |
| **the next session in each repo** | `SB-N` | ⚑ **every symbol set living only in transcript** | ⚑ **a rostered party that will not know it** |

⚑⚑⚑ **`SB-N`'s SECOND NOMINATION IS THE ONE THAT GENERALISES AND NO OTHER LEG MADE IT.** Per
census-kit `§6`, a party is a *vantage*, and a compaction is a lossy channel. **`SB-2` measures that
substrate's actual work list — the ⟡/Ⓝ/Ⓑ symbols — lives only in transcript**, with a gated
mechanical home (`scripts/worklist.py`) that is itself blocked by a per-file mypy gate. `MT-02`
reports the same shape (*a cron prompt's reified symbol list*) and `PK-02` reports it a third time
(*a list I maintain by hand, in a cron prompt*). **Three legs' ledgers live in a channel that
erases them, and only one leg named the party that will inherit the loss.**

---

## `AX-10` Residue — what I could not do, kept rather than closed

1. ⚑ **I could not verify any leg's internal figures.** Every count in `AX-2` is that leg's
   testimony, carried per `§C`. I re-ran none of the 8 repos' producers, and doing so would have
   required 8 trees and 8 sets of credentials. **`AX-2` is a table of claims, not of measurements**,
   and I have marked it so rather than laundering it into an apex figure.
2. ⚑⚑ **The 65 operator-edges are unverifiable in both directions.** The operator is the census's
   largest blocking vertex and the only party who could confirm or deny a single one of them. **A
   census that rosters only the surveyed parties cannot measure its own dominant edge.** This is
   `§6`'s roster-completeness failure at the level of the *convening decision* rather than the party
   list — and `§6` says a nomination is the only mechanism by which a survey discovers its index was
   incomplete. **No leg nominated the operator, because no leg thought of the operator as a party.**
3. ⚑ **`AX-3`'s edge counts are hand-built and I hold no instrument.** I read 8 legs and built the
   relation by hand. **A second reader would produce a different `27`** — the boundary between "a
   row" and "an edge" is a judgement I made ~90 times. `MT-06`-3's shape: a correct count over a
   population I named. I state the method rather than defend the number.
4. **`AX-5c`'s repair is unfilable.** `§D` forbids amending `§V` rev 12, and rev 13 already
   established the precedent of a new row rather than an edit. **The control is wrong and the
   conclusion is probably right, and both stand.**
5. ⚑ **I do not know what protects against `§V` rev 16's channel failure** — a correct control
   transmitted and discarded in the writing down. `summit` said it did not know; the dispatcher said
   it did not know. **Nor do I.** ⚑ *Recorded as residue for the third time in this run, by the third
   party to reach it, which is itself the strongest evidence that it is real and unsolved.*

---

## `AX-11` Termination

**Every one of the 47 leg items is either in `A` (3 merged as `A-ITEM`) or carried in `AX-2`'s
glue.** No item was dropped for being unshared; no identification was made without a witness from
`§3`'s admissible list; nine near-misses are recorded as non-identifications with reasons. The
output is larger than the largest leg.

⚑ **What this document is NOT:** a total, a ranking, an adjudication of any divergence, or a
verification of any leg. Four divergences stand unresolved with both sides' denominators. Twelve
`§Q`-6 rows were discharged by computing the bipartite relation once; **twenty-one remain, and seven
of those have no schema in any party's tree.**
