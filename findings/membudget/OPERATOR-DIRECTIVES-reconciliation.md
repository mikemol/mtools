# Operator directives — the four-vantage reconciliation

**What this is.** The glue map over the directive censuses in this directory. Compiled 2026-09-06 by
`linux-sources`, who **also filed a leg** (`OPERATOR-DIRECTIVES-linux-sources.md`, frozen before this
pass began and not edited during it, except for a header pointer added after).

> ⚑ **REVISED 2026-09-06 (rev 2) against `census-kit` §3/§A0/§A6, which was written from this run and
> names this file as its measured instance of over-gluing.** Rev 1 declared *"nothing is dropped for
> being unshared"* and then made two unwitnessed identifications: it seated **two of my own sweeps in
> leg position** and let their numbers silently supersede a leg's. **Stating the construction did not
> produce it.** What changed: a tiered source table (below), a witness column on the span (§5), a
> divergence register (§4a), and §6's bound on what re-sweeping buys. **No directive quotation was
> altered.**

## Reference arms

- **Evidence arm** — four transcript corpora (linux-sources, paperkit, substrate, cassian ×2
  directories), plus four filed documents in this directory. Everything else, including
  `~/.claude/projects/-home-mikemol-github-mtools/`, is **UNAVAILABLE, not absent**.
- **Normative arm** — `census-kit` (`~/.claude/skills/census-kit`), installed 2026-09-06.
  ⚑ **Audited before adoption, per its own §4.** It is derived from *this run*, so it is **not a
  decorrelated reviewer of it**: its rules are checkable on their merits, its account of who did what
  is testimony. One of its predecessor's recommendations — *state the categorical direction in beat
  1* — its own postmortem finds unactionable by construction. I adopt its §3 witness rule and §A6
  re-sweep rule because I verified both against this file's actual defects; I do not adopt its
  narrative of the run as citation.

**Sources — TWO TIERS, and the distinction is the repair.**

⚑ **A leg is a document a party filed about its own corpus. A sweep is my reading of someone else's
corpus.** Rev 1's table put these in one column. They are different epistemic objects: a leg is
testimony from the vantage that holds it; a sweep is my inference about a vantage I do not occupy.

**Tier 1 — filed legs** (primary citation for their own content; I did not author them):

| leg | file | IDs | filed by |
|---|---|---|---|
| linux-sources | `OPERATOR-DIRECTIVES-linux-sources.md` | `A1`–`A17` | linux-sources *(me — see disclosure)* |
| paperkit | `OPERATOR-DIRECTIVES-paperkit.md` | `P1`–`P15` | paperkit |
| cassian-observability | `OPERATOR-DIRECTIVES-cassian-observability.md` | `C1`–`C15` | cassian-observability |

| substrate | `OPERATOR-DIRECTIVES-substrate.md` | `S8`–`S15` *(their numbering)* | substrate |

**Tier 2 — apex-authored sweeps** (mine; **not** legs, and not corroboration of any leg):

| sweep | file | IDs | status |
|---|---|---|---|
| cassian's corpus, re-swept | `OPERATOR-DIRECTIVES-cassian-observability-antecedents.md` | `Q1`–`Q14` | ⚑ **overlaps cassian's leg's corpus.** Where our numbers differ, §4a D1 carries both. |
| substrate's corpus, swept | *(this file, §2, §3, §5)* | `S1`–`S22` *(MY numbering)* | ⚑⚑ **CONTRADICTED by substrate's own leg on five directives — §4a D0.** Carried as apex testimony, not as substrate's vantage. |

⚑⚑ **TWO `S`-NUMBERINGS EXIST AND THEY ARE NOT THE SAME OBJECTS.** Substrate's leg uses `S8`–`S15`
for the 8 directives it actually received; my sweep used `S1`–`S22` for what I attributed to it.
**Same prefix, different referents** — precisely the merged-namespace over-glue `census-kit` §A0 says
is over-gluing in its purest form, and I created it by numbering another party's corpus in my own
scheme. **Substrate's numbering is authoritative for substrate.** Mine is retained only so rev 1's
citations remain traceable, and every `S*` in §5 should be read as *my claim*, not substrate's.

⚑ **This is a PUSHOUT over tier 1, with tier 2 carried as apex testimony.** Every vantage-local
directive is carried whole. Nothing is dropped for being unshared — and, per rev 2, nothing is
*promoted* for being mine.

---

## 1. ⚑ THE HEADLINE: the operator substitutes the addressee out of the recipient list

**Four copies of one directive, none identical.** The kickoff, 2026-09-05 ~15:36–15:39:

| received by | "Also see how … is using the machinery" |
|---|---|
| linux-sources `A1` | substrate, paperkit, cassian-observability |
| paperkit `P1` | substrate, paperkit, **linux-sources**, cassian-observability |
| cassian `C1` | substrate, paperkit, **linux-sources** |
| substrate `S5` | **cassian-observability** *(alone)* |

The first-order filing order shows the same rotation:

| received by | "I want you, …, to write your exhaustive findings" |
|---|---|
| linux-sources `A2` | you, substrate, paperkit, cassian-observability |
| paperkit `P2` | you, **linux-sources**, substrate, cassian-observability |
| cassian `C4` | you, substrate, paperkit, **linux-sources** |
| substrate `S7` | you, **linux-sources**, paperkit, cassian-observability |

⚑ **Each delegate is named to the other three and elided from its own copy.** Read from inside one
transcript this is an *asymmetric* instruction — "survey those three" — and every delegate
reconstructed it that way. **The symmetry exists only across vantages**, and no single-vantage census
could recover it. Cassian's `§D1` named this from two vantages; four confirm it and add substrate's
copy, which is the outlier.

⚑ **Substrate's copy is the one worth flagging separately.** It names *one* peer, not three.
Substrate authored the tool, and its kickoff pointed it at exactly one consumer. Whether that shaped
`substrate.md`'s scope is not answerable from the transcripts — but the input differed, and no filing
records that it did.

**And the framing directive (15:01, coordination partners) shows the same substitution:**

| received by | "You will be coordinating with …" |
|---|---|
| linux-sources | `substrate` |
| cassian | `substrate` and `linux-sources` |
| substrate | `linux-sources` *(15:01:50)*, then `cassian-observability` *(15:05:50)* — two messages |

---

## 2. ⚑ THE SECOND HEADLINE: the exclusion axis was the ORIGIN, not a reframing

**The census's own central finding was that four delegates audited the wrong axis** — they surveyed
*resources* when the operator wanted *exclusion*. Two peers relayed the 16:33 reframing (`A10`/`C11`/
`P9`/`S16`) as the moment this was revealed, and cassian's `§B1` opens *"THE OPERATOR HAS STATED THE
CENSUS'S ACTUAL PURPOSE AND ALL FOUR OF US AUDITED THE WRONG AXIS."*

⚑ **The antecedent sweep refutes the "reframing" reading.** `Q7`, **2026-08-29T22:16:35Z**, seven
days before the kickoff, answering a block of the delegate's own prose about **port collisions
between concurrent forks**:

> Neat! Means you need to genericize the memory lease logic for resource-lease.

**The trigger was two workers wanting the same exclusive name.** Not memory. Not CPU. That one
sentence created `scripts/resource-lease` — the artifact cassian's entire census position is built
on — and it is **quoted in none of the four findings files**.

**Corroborated by two further antecedents:**

- `Q6`, **2026-08-27T12:50:28Z** — the pgbouncer consult, and the only place *"That way I don't have
  to trust client-side throttling"* appears **in the operator's own words** (every other record is a
  peer relay): *"Is there a pgproxy which can hold inbound connections and only admit a limited
  number of concurrent connections through?"* ⚑ **The admission-gate shape is fully formed** — hold
  inbound work, admit a bounded number, don't trust the client to self-limit — nine days early, about
  database connections instead of agents.
- `S3`, **2026-08-31T19:18:24Z**, to substrate alone — the RAII-by-stack-address account that becomes
  substrate's `pid:starttime` LOTO headline: *"spans were tagged with the stack address of the
  allocated RAII object … guaranteed not to overlap with another entry/exit event that had the same
  ID."*

⚑ **So the four delegates' shared conclusion is true about their reading and false about the
intent.** The exclusion axis was not introduced at 16:33; it was restated there. The evidence sat in
cassian's and substrate's transcripts the whole time, outside every census's date bound.

⚑ **This is the census's own error class, one level up.** Four parties measured a population
(09-05) and reported a fact about the subject. `Q7` was constructible, reachable and observable the
entire time — it was simply outside the window everyone chose.

---

## 3. Directives no other delegate received

⚑ **Carried whole, per the pushout.** A directive given once is not noise.

### To cassian only

| id | when | what |
|---|---|---|
| `C2` | 09-05 15:44 | *"This host is _designed_ to swap. It uses zram and zswap."* — answers a hypothetical only cassian raised |
| `C3` | 09-05 15:46 | *"mat230 repo is retired."* — corrects a survey set only cassian named |
| `Q4` | 08-24 16:07 | *"membudget now has a load-aware halting behavior."* — a capability the delegate did not know it had |
| `Q5` | 08-27 12:21 | *"substrate doesn't know it, but it's already throttling if it's routing through membudget"* |
| `Q9` | 08-30 01:15 | *"I thought we _generalized_ the leasing mechanism, though? The RAII-by-liveness needs to be _there_."* |

⚑ `Q9` is the ruling `cassian-observability.md` cites in its bias disclosure but quotes only as a
docstring paraphrase. **The ruling is a question**, and it is the same question as the kickoff
grievance: *why is this being re-derived instead of shared?*

### To paperkit only

| id | when | what |
|---|---|---|
| `P12` | 09-05 17:00 | ⚑ **The third-order brief re-issued byte-identical**, ten minutes after the pushout correction |
| `P15` | 09-05 17:16 | *"I think your CONSOLIDATED.md needs to be paperkit-consolidated.md; everyone else's is `<repo>-consolidated.md`."* |
| — | 08-22 21:53 | the doubling ladder: *"If we _default_ to something like 8MiB, and then double every time we have an OOM event"* + the discriminator requirement |
| — | 08-25 11:44 | *"This is literally what UUIDs are for."* — the fix `paperkit.md` §2(a) credits |
| — | 08-27 08:14 | ⚑ **the seed**: *"the box is supposed to expand its memory into zram and zswap. Talk with cassian-observability and linux-sources on how/why we had an oom"* |

### To substrate only

| id | when | what |
|---|---|---|
| `S6` | 09-05 15:42 | *"Also dispatch a transcript miner to go over membudget mentions."* |
| `S3` | 08-31 19:18 | the RAII-by-stack-address account (see §2) |
| `S1` | 08-27 12:28 | *"membudget has a concurrency limit built-in … set to a concurrency limit of 10 by default"* |
| `S2` | 08-27 12:31 | *"Bull. Because that load limit was put there _because_ the concurrent load was crashing the box."* |
| `S2b` | 08-27 12:32 | ⚑ *"enough concurrency thrown at the postgres backend _caused kernel hangs_. **This isn't recorded in the source.**"* |

⚑ `S2b` is the fragmentation thesis stated as a concrete loss: a constraint's *reason* lived in a
generated makefile, an agent rewrote the generator, and the reason went with it.

### To linux-sources only

| id | when | what |
|---|---|---|
| `A9` | 09-05 16:30 | *"`--remote_local_fallback` Yeah, don't do that; it evades the scheduler and consumes resources against the very same machine the scheduler is protecting."* |
| `A11` | 09-05 16:34 | *"(From my perspective, I'm watching the audit settling, and relaying my corrections to all parties involved)"* |
| `A0` | 08-30 00:09 | *"Ask cassian-observability how it solved _exactly thi problem_ by expanding on membudget's capabilities."* |

⚑ **`A11` is the operator naming their own role in the mechanism §4 measures.**

---

## 4. The operator as a channel between sessions

⚑ **Measured, not inferred.** The operator repeatedly quoted **one delegate's prose into another
delegate's session** as the block being answered:

- `A4`/`C6` — the same delegate sentence (*"The nine refusals are its own ratchet family…"*) quoted
  back to **two** sessions.
- `A8`/`C10`/`P8`/`S15` — the same sentence (*"Bazel IS the semaphore is true within one build and
  false across repos"*) answered in **four** sessions with the same words, **typo `evveryone` and
  lowercase `Sortof` intact**.
- `A15`/`C15`/`P14` — the consolidating directive quotes identical delegate prose in three
  transcripts, **~6 minutes apart** between cassian (17:07:47) and linux-sources (17:13:34).

⚑ **Cassian's `§D4` flagged that 6-minute gap as unresolved** — either two delegates independently
produced the same paragraph, or one was relayed. **The antecedent file's relay ledger `§C P2/P3`
breaks the symmetry**: the operator demonstrably relays one delegate's words into another's session.
The relay branch has direct support; the independent-production branch has none. Not proof — but the
branches are no longer symmetric, and this reconciliation does not need to resolve it to record it.

### ⚑ Relays degrade in transit — three measured instances

A peer relay is evidence a directive exists. **It is not that directive**, and here is why:

| relay | what changed |
|---|---|
| cassian → paperkit, on `A8` | **typo normalized** — operator typed `evveryone`, relay said `everyone` |
| cassian → substrate, on the first-order order | ⚑ **recipient list permuted** — relayed as *"you, substrate, paperkit, and linux-sources"* when substrate's own copy reads *"you, linux-sources, paperkit, and cassian-observability"* |
| cassian & paperkit → substrate, on `A10` | **bracket inserted** — *"the whole reason I told [you] to collect…"* |

All three were presented as **"Verbatim."** ⚑ **The quotation marks survive; the content shifts.**
Same shape as the hedge-does-not-survive-the-relay finding — and here it is measured on the operator's
own words, three times, by three different relayers.

⚑ **The `mtools-2c` relay carries a framing that appears in no transcript at all**: *"membudget
encodes a LEASING model — 'a kind of genericized RCS/CVS.'"* That phrasing reaches substrate only
second-hand and originates in the mtools session directory, which no census swept. **UNAVAILABLE, not
absent.**

---

## 4a. ⚑ Divergence register — where a sweep and a leg disagree

**New in rev 2.** Rev 1 had no such section, which is why the disagreements below were resolved
silently in my own favour. **Both branches stand. Resolve nothing the witnesses do not resolve.**

### D1 — cassian's corpus: one file, or five?

| | cassian's leg (`C`) | my sweep (`Q`) |
|---|---|---|
| corpus claimed | *"**One transcript**: `d90455f2…`, ~377 MB"* | 5 files across **2 directories**, 411,466,345 bytes |
| `queued_command` in `d90455f2` | 1,272 | 1,274 |
| …`origin.kind: human` | 476 | 477 |
| unparseable lines | 10 | 10 |

⚑ **Rev 1 called this "independent corroboration" and attributed the delta to a counting rule.** That
was an adjudication, and it ran in my favour without a witness. **The honest statement:** the two
counts differ by 2 records; I hold a counting rule that explains it; **cassian has not confirmed
that rule.** The bad-line count agreeing exactly is the strongest shared datum and it is genuine.

⚑ **The corpus-scope difference is not a delta, it is a different denominator.** Cassian's leg is a
sound census **of one file**; my sweep read five. Neither is wrong. **A leg that bounded itself to
one transcript has not made a claim about the other four**, and rev 1's framing let my larger
denominator read as a correction to their smaller one. It is not — it is a different measurement.

### D2 — the ~6-minute consolidate gap (§5's outlier row)

- **cassian's leg (§D4):** *"Unresolved from this vantage; naming it is what a second vantage buys."*
  Two branches — independent production, or relay — held symmetric.
- **my sweep (`Q`/§C P2–P3):** the relay branch has direct support; the independent branch has none.

⚑ **Carried unresolved.** My sweep makes the branches **asymmetric**, and *asymmetry is not proof*.
Cassian's non-resolution stands as filed. Settling it needs the mtools corpus, which nobody read.

### ⚑⚑ D0 — SUBSTRATE'S LEG CONTRADICTS MY `S`-SERIES SWEEP, AND ITS LEG IS THE BETTER WITNESS

**The most consequential divergence in this file, and rev 1 could not see it** because it treated my
sweep as substrate's vantage. Substrate's own leg reports that **it never received five directives my
sweep placed in its column**:

| directive | my sweep says | substrate's leg says |
|---|---|---|
| BES / `--config=remote` | `S15` 16:24:17 | ⚑ **ABSENT** |
| ⚑ *"I said pushout, damnit, not pullback"* | `S18` 16:54:04 | ⚑⚑ **ABSENT** |
| *"Audit your rewrites…"* | `S19` 17:05:18 | ⚑ **ABSENT** |
| first-order filing order | `S7` 15:47:10 | ⚑ **ABSENT** |
| CPU-vs-memory axis history | `S10` 16:20:01 | ⚑ **ABSENT in the census window** |

**Substrate's census window holds 8 directives against paperkit's 15.** Its leg states the
consequence plainly: its third-order and consolidated filings were written *"without ever receiving
the pushout/pullback correction, the audit order, or the naming correction — which means any
pushout-correct content in `substrate-third-order.md` arrived by peer relay or by substrate reading
the other filings, not by operator instruction."*

⚑ **I do not adjudicate this, but the asymmetry is stateable.** Substrate's leg used a
purpose-built reader (`scratch/transcriptstruct.py`) over **all seven files, ~1.48 GB**, and warns
that grep fails on this corpus because the text lives inside JSON-escaped strings. My `S` sweep read
a subset and was term-filtered. ⚑ **A party's own leg reporting an absence in its own corpus, with a
denominator and a named instrument, outweighs my sweep asserting a presence** — but *outweighs* is
not *settles*, and **both branches stand** until someone re-runs substrate's reader against my
claimed locators.

⚑ **This is precisely the failure `census-kit` §3 names**, and rev 1 committed it: *"an apex row
cites a leg for a claim that leg denies."* Rev 1 cited substrate for five directives substrate says
it never got. **The witness column would have caught it** — none of those five rows had one.

### D3 — what cassian's leg declares UNAVAILABLE that my sweep then read

Cassian's §D5 declares substrate's and paperkit's transcripts unread, and **deliberately** did not
read `OPERATOR-DIRECTIVES-paperkit.md` *"to keep this an independent vantage rather than a merge."*

⚑ **That is a methodological choice, not a gap, and rev 1 treated it as one.** My sweeps read all of
it. **Reading more does not retroactively improve cassian's leg** — it makes mine a *different,
non-independent* instrument. Cassian's deliberate blindness is what makes its agreement with my
sweep worth anything at all; had I "fixed" it, the corroboration would have evaporated.

---

## 4b. ⚑ Roster accounting

**New in rev 2.** Per `census-kit` §6: a party that never filed is a **remainder entry with a
reason**, not a silent omission — otherwise the span is quietly computed over `N−1` legs.

| party | status | evidence |
|---|---|---|
| linux-sources | **filed** | `OPERATOR-DIRECTIVES-linux-sources.md` |
| paperkit | **filed** | `OPERATOR-DIRECTIVES-paperkit.md` |
| cassian-observability | **filed** | `OPERATOR-DIRECTIVES-cassian-observability.md` |
| substrate | ⚑ **FILED** — see correction below | `OPERATOR-DIRECTIVES-substrate.md`, mtime **2026-09-05 22:13:16** |
| `mtools-2c` / `mtools-05` | ⚑ **NEVER ASKED** | not on the roster; §6 shows ≥2 operator rulings live only in its session |

### ⚑⚑ ROSTER CORRECTION, CAUGHT MID-REVISION — and it is this file's third instance of one error

**Rev 2 first wrote substrate as `NO RESPONSE`.** Substrate's leg was on disk at **22:13:16**, before
rev 1 was even written. ⚑ **I asserted an absence from a directory listing I had taken earlier and
never refreshed** — the file was there the whole time, and my instrument was a stale `find`.

**That is the same defect three times in one document, at three scales:**

| # | the absence I asserted | what it actually was |
|---|---|---|
| 1 | rev 1: substrate has "no standalone file" | it had one, filed before rev 1 |
| 2 | rev 2 §4b first draft: substrate `NO RESPONSE` | a stale listing, not a measurement |
| 3 | the whole census (§2): the exclusion axis was "a reframing" | a window nobody set |

⚑ **`census-kit` §5 exists for exactly this and I did not apply it to my own roster.** A negative
finding counts only what the query could see; my query was a directory listing with a timestamp older
than the fact it was used to deny. **A positive control — re-listing the directory — cost one call
and I ran it only because a size check happened to surface the file.**

**Two roster defects remain real:** the roster never included **mtools**, which holds directives every
other party could only receive by relay; and *"they had nothing"* vs *"never asked"* is still
undistinguished for it. **A survey run over the wrong index computes the wrong glue.**

---

## 5. The shared spine — the arc all four received

Timestamps are **receipt**, per-delegate. ⚑ **No transcript records send time**, so the operator's
actual typing order across four delegates is not recoverable from any record or from all of them.

⚑ **This is the span `A`, and every row now carries a witness** (rev 2). Rev 1 printed the same
correspondences with no warrant for any of them — *"an identification with no witness is not a
finding about the world; it is the apex's convenience."*

**Witness key.** `BYTE` = quoted directive text byte-identical across the cited legs.
`RECEIPT<n>` = all receipts within `n`, consistent with one dispatch pass. `PROSE` = the same
delegate prose quoted in each as the block being answered. ⚑ `S*` columns are **apex sweep, not a
leg** — they are corroborated by nothing, and a row whose only support is `S*` is marked so.

| beat | linux-sources | paperkit | cassian | substrate | witness | text |
|---|---|---|---|---|---|---|
| kickoff | `A1` 15:38:11 | `P1` 15:39:10 | `C1` 15:37:51 | `S5` 15:36:28 | ⚑ **RECEIPT2m42s only — NOT byte-identical** | *"dispatch a research agent to study membudget _in depth_ … It's genericized. Or it's supposed to be."* |
| first-order | `A2` 15:47:00 | `P2` 15:49:18 | `C4` 15:46:49 | `S7` 15:47:10 | ⚑ **RECEIPT2m29s only — NOT byte-identical** | *"write your exhaustive findings re membudget into the mtools repo"* |
| second-order dispatch | `A3` 15:59:47 | `P3` 16:00:08 | `C5` 15:59:45 | `S8` 15:59:41 | BYTE + RECEIPT27s | *"read through all of the findings … and reconcile with your own"* |
| second-order filing | `A5` 16:08:48 | `P5` 16:08:59 | `C7` 16:08:42 | `S9` 16:08:54 | BYTE + RECEIPT17s | *"…as files distinct from their first-order findings"* |
| axis history | `A6` 16:20:07 | `P6` 16:19:58 | `C8` 16:20:05 | `S10` 16:20:01 | BYTE + RECEIPT9s | *"CPU became the primary constraint, but membudget had the rigorous and reliable gating logic"* |
| shared host | `A7` 16:21:59 | `P7` 16:22:04 | `C9` 16:22:11 | `S11` 16:22:24 | BYTE + RECEIPT25s | *"all of the consumers of membudget run on the same physical host"* |
| BES | `A8` 16:25:23 | `P8` 16:25:19 | `C10` 16:25:11 | `S15` 16:24:17 | BYTE (incl. typo `evveryone`, lowercase `Sortof`) + PROSE + RECEIPT66s | *"this is why I'm pushing evveryone to use the same BES/--config=remote target"* |
| ⚑ LOTO reframe | `A10` 16:33:10 | `P9` 16:33:07 | `C11` 16:32:48 | `S16` 16:33:04 | BYTE + PROSE + RECEIPT22s | *"the lease-shape phenomenon generalizes very well to 'multiple agents modifying the same tree…' Granular lock-out/tag-out."* |
| third-order | `A12` 16:49:46 | `P10` 16:50:00 | `C12` 16:49:44 | `S17` 16:49:41 | BYTE + RECEIPT19s | *"establish what a pushout looks like"* |
| ⚑ correction | `A13` 16:54:08 | `P11` 16:54:11 | `C13` 16:54:06 | `S18` 16:54:04 | BYTE + RECEIPT7s; **cassian re-derived its stamp directly** (leg §E) | *"I said pushout, damnit, not pullback."* |
| audit | `A14` 17:05:44 | `P13` 17:05:49 | `C14` 17:05:21 | `S19` 17:05:18 | BYTE + RECEIPT31s | *"Audit your rewrites and finding documents for consistency, correctness and convergence."* |
| consolidate | `A15` 17:13:34 | `P14` 17:09:02 | `C15` 17:07:47 | `S20` 17:08:57 | BYTE + PROSE; ⚑ **RECEIPT 5m47s — the outlier** | *"we don't want to hold the retractions, we want to hold the results"* |

### ⚑ Two rows are NOT identified by byte-identity, and rev 1 hid that

**The kickoff and first-order rows are the two whose text demonstrably differs per delegate** — that
is §1's entire finding. Seating them in a span table that reads as *"same directive, four receipts"*
asserts an identity the evidence contradicts.

They are identified as **one dispatch event with four different texts**, on receipt clustering plus
shared subject — a weaker warrant than every other row, and now labelled as such. ⚑ *A row whose
witness is receipt-only cannot support a claim about wording.* §1 is the correct place to read those
two; this table is not.

⚑ **The correction spans 16:54:04 → 16:54:11 — seven seconds across four delegates.** One pass, all
parties. The audit beat spans 28 seconds. **The consolidate beat spans 5m47s**, which is the anomaly
§4 discusses and the only beat whose spread is not seconds.

⚑ **`A13` lands between the third-order order and the third-order files in every transcript.** Any
third-order document that constructs a pullback is answering the directive as first read.

---

## 6. What this census does NOT cover — the union of four honest bounds

⚑ **Every leg named its own weakest edge. They are not independent, and that matters.**

**Unparseable lines, unrecovered, by leg:**

| leg | bad lines / total |
|---|---|
| linux-sources | 2 / 89,906 |
| paperkit | 1 / 93,912 |
| substrate | 15 / 441,721 |
| cassian (both dirs) | 11 / 154,808 |
| **union** | **29 / 780,347** |

No claim is made that these carried nothing.

⚑ **THE SHARED BLIND SPOT — one region measured four times, not four confirmations.**
**Tool-call interiors were excluded by construction in all four sweeps.** Operator words quoted
inside a subagent brief or a `tool_use` input would not surface in any of them. Cassian's leg states
it exactly: *"Three independent sweeps sharing one blind spot is **not** three confirmations — it is
one unexamined region measured three times."* Four does not improve it.

**Corpora not swept at all:**

- ⚑ **`-home-mikemol-github-mtools/`** — the `mtools-2c` and `mtools-05` sessions. §4 shows at least
  two operator rulings were typed there (*"a kind of genericized RCS/CVS"*, *"mtools takes the
  STRICTEST UNION of all peer repos' linting + hooks"*) and reach every other corpus only by relay.
  **This is the largest known gap in this reconciliation.**
- Any of the other ~48 project directories.
- Anything said outside a Claude session.

**Measured negatives — searched, and empty:**

- ⚑ `-home-mikemol-github-cassian-obs/` — 1 file, 4,684 lines, 33 human queued_commands, **all from a
  2026-07-27 session, zero membudget directives.** The two cassian spellings are not two halves of
  one corpus. *An absence that has been measured is a different artefact from an absence nobody
  looked for.*
- **AskUserQuestion standalone answer records: zero, across all four corpora.** The one
  answer-shaped artefact (15:01) carries its questions and answers inlined in a single operator
  message and is quoted whole in three legs.
- **Sidechain records carrying directives: zero** (cassian's leg checked explicitly).

---

## 7. ⚑ THE METHODOLOGICAL FINDING: `queued_command` is invisible to the obvious scan

**Independently hit by all four sweeps.** Operator messages typed *while a delegate is mid-turn*
arrive as `type: "attachment"` with `attachment.type: "queued_command"` and `message: null`. **A scan
keyed on `message.role == "user"` does not see them and reports a complete, coherent, plausible
arc.**

| leg | directives only in `queued_command` shape |
|---|---|
| linux-sources | **5 of 17** |
| paperkit | **3 of 15** |
| substrate | **11 of 29** in-window |
| cassian | **11 of 15** — a role-keyed scan returns a **4-item** arc and reports it complete |

⚑ **The directive that ordered the findings written at all exists in NO other shape** — in
linux-sources', substrate's, *and* paperkit's transcripts. A census that missed this shape would
report the membudget filings as having no originating directive, and would look complete doing it.

**Three sub-traps, each measured:**

1. ⚑ **`origin` is nested inside `attachment`, not at the record's top level.** Substrate's sweep
   probed the top level and got **`qc_human: 0` across all 2,666 records** — its own words:
   *"a clean, plausible zero — a silent false absence, not an error."* Corrected path yielded
   **1,160**.
2. ⚑ **The human text is in `attachment.prompt`, which is a LIST of content blocks** — not
   `command`/`text`/`content`. `str()`-ing it yields a dict repr that keyword-matches on **field
   names** rather than content.
3. ⚑ **User-role records carrying `tool_result` blocks are tool output wearing the user role.**
   Cassian's leg: without that filter, **1,282 of 1,700** candidates are tool output.

⚑ **AND THE RECOVERY WAS NOT SELF-FOUND IN AT LEAST ONE LEG.** Cassian's file records this plainly
rather than presenting a clean result: its arc went from 4 to 15 only because *the operator relayed
another repo's observation mid-task*. Its own note: *"the alternative record — a complete-looking
census with no note of how it got complete — would misrepresent the method as sound when it was
rescued."*

⚑ **The general form: a scan covers the shapes it knows to look for and reports that as coverage of
the QUESTION.** Nothing about a 4-item result announces itself as partial. This is the same class as
§2 (a date bound reported as a subject fact) and as the census's own subject matter.

---

## 8. For the next survey — what the record supports

⚑ **Derived from the measurements above, not from preference.** Each carries the observation that
warrants it.

1. **Name every participant identically in every copy, including the addressee.**
   *Warrant:* §1 — four copies, four recipient lists, and every delegate reconstructed an asymmetric
   ask it could not detect from inside.

2. **State the generalization target and the categorical direction in beat 1.**
   *Warrant:* the LOTO reframe (`A10`) arrived **after two rounds were already filed**, and
   *"I said pushout, damnit"* cost a full re-issue of the brief (`P12` re-issued byte-identical ten
   minutes later). Substrate's sweep offers the same read unprompted: stating both up front would
   likely collapse beats 3 and 4 into one.

3. **Order the consolidated document up front, not as beat 5.**
   *Warrant:* `A15`/`C15`/`P14` arrived as an afterthought correcting a forward-pointer proposal;
   four delegates had already written retraction-carrying documents.

4. **Set the time window explicitly, and justify it.**
   *Warrant:* §2. Every first-order filing was date-bounded to the census day by nobody's
   instruction, and the directive that created the artifact under study (`Q7`) sat seven days
   earlier. **The antecedents were load-bearing and no filing carried them.**

5. **Name the transcript shapes in the brief.**
   *Warrant:* §7. `queued_command` / `attachment.origin.kind` / `attachment.prompt`-as-list /
   `tool_result`-wearing-user-role. Four sweeps hit all four traps; the one that was briefed on them
   (paperkit) still found the shape independently and confirmed the trap list.

6. **Expect relays to degrade, and treat a relayed quote as testimony.**
   *Warrant:* §4 — typo normalized, recipient list permuted, bracket inserted, all labeled
   "Verbatim."

7. **If a delegate is both author and surveyor of the subject, say so in its brief.**
   *Warrant:* §1 — substrate authored membudget and its kickoff named one peer rather than three.
   `substrate-consolidated.md` opens with a self-declared bias line; the *input* asymmetry is not
   recorded anywhere.

8. **Sweep the coordinating repo's own session directory.**
   *Warrant:* §6 — `mtools/` was never swept, and at least two operator rulings live only there.

---

## 9. Provenance of this file

**Built from four sweeps, three of them dispatched by `linux-sources` 2026-09-06 against
substrate's, paperkit's and cassian's transcript corpora, plus its own earlier sweep.** Two of the
four leg-files (`-paperkit`, `-cassian-observability`) were written by **those delegates'** own
sessions before these sweeps ran, on the same operator directive (`Q13`/`Q14`).

⚑ **CORRECTED IN REV 2 — the corroboration claim was half right and the half that was wrong is the
one that mattered.**

Rev 1 said the sweeps reproducing those files byte-for-byte were *"two witnesses that could have
disagreed."* **Split the two cases:**

- ⚑ **paperkit: genuine corroboration.** Its leg was filed by paperkit's own session, and my
  independent sweep of paperkit's corpus reproduced it. Different session, different brief, same
  result. **These could have disagreed.**
- ⚑ **cassian: NOT corroboration, and rev 1 said it was.** My `Q` sweep read cassian's corpus and
  reproduced cassian's leg — but *"a leg re-swept rather than read is one witness, not two."* Both
  readings are of **the same bytes**; agreement between two readings of one corpus tests my reader,
  not the finding. Where we differed (§4a D1) rev 1 resolved it toward my numbers and called that
  corroboration too, which inverts the error.
- ⚑ **substrate: no witness at all.** Not one witness, not two — **zero**, since substrate never
  filed. `S1`–`S22` is a single unreviewed reading.

**So the corroboration is: one genuine cross-witness (paperkit), one same-corpus re-read (cassian),
one uncorroborated sweep (substrate).** Rev 1 reported all three as the first kind.

⚑ **What this file adds that no leg holds:** §1 (the substitution, visible only across four),
§2 (the antecedent refutation of the wrong-axis conclusion), §4's relay-degradation table, §5's
receipt-time spread, and §7's four-leg trap census.

⚑ **What it does not do:** it does not supersede any leg. Each leg is the primary citation for its
own directives; this is the glue map. A reader tracing one directive should read the leg.

---

## 10. ⚑ What rev 2 changed, and what it could not fix

**Changed:** the source table split into filed legs vs apex sweeps · reference arms declared · §4a
divergence register added · §4b roster accounting added · §5's span given a witness column, with the
two receipt-only rows marked · §9's corroboration claim split three ways.

**Not changed:** every directive quotation, every timestamp, §1's substitution finding, §2's
antecedent finding. **Rev 1's content was not wrong; its warrants were missing** — which is the
distinction the census itself was about, arriving in its own record.

⚑ **What rev 2 cannot fix, stated rather than papered over:**

1. ⚑ **SUPERSEDED MID-REVISION — substrate HAD spoken.** This slot first read *"substrate still has
   not spoken."* Its leg was on disk before rev 1 existed (§4b correction), and it **contradicts my
   `S`-series on five directives** (§4a D0). ⚑ **The residual gap is the inverse of what I wrote:**
   not that substrate is silent, but that **my sweep asserted presences its own vantage denies**, and
   nobody has re-run substrate's reader against my locators to settle it. `S1`–`S22` is now
   **testimony contradicted by a better-instrumented leg**, carried unresolved.
2. **mtools was never on the roster** and holds directives nobody else can cite.
3. **The tool-call-interior blind spot is unchanged** — one unexamined region measured four times.
4. ⚑ **This revision was prompted by an artifact derived from this very run.** `census-kit` is not a
   decorrelated reviewer of the work it corrected; it is this run's own postmortem wearing a
   normative face. It found a real defect — I verified the over-glue against my own file before
   accepting it — but *"the corrector was downstream of the thing corrected"* is a bound on how much
   independent confirmation this rev 2 represents. **A second party re-deriving §4a from the legs
   would be worth more than this paragraph.**
