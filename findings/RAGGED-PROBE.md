# Ragged probe

⚑ THE P-ARM. Same file, same table, the pipe written out in words instead of as a byte. This
must PASS, or the refusal above measured a gate that refuses everything rather than a gate that
reads raggedness.

| surveyor | prefix | file |
|---|---|---|
| alpha | AL- | a row naming a pipe byte in words inside a code span |
