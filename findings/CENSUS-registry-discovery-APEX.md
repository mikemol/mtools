# APEX: registry-discovery — the span, then the glue

⚑ **THE BRIEF IS `gcalculus:proceedings/census/registry-discovery.md` (r3, `361694f`); THE INDEX
IS `findings/CENSUS-registry-discovery.md` (frozen at `9ea2b53`).** This file is the apex: phase 1
publishes the span `A`; phase 2 glues along it and carries every remainder. Written by `mtools-ec`,
successor to the dispatcher `mtools-9f`, after the freeze and not before — the legs were unread by
any mtools session until 2026-09-20.

## §0 Reference arms (§4), declared before reading

**Evidence arm — five legs, each pinned to the commit read:**

| leg | prefix | pinned at | answered brief rev |
|---|---|---|---|
| gcalculus `proceedings/census/leg-gcalculus.md` | `GC-` (Q1–Q5) | `9d4ab3f` (r4, 2026-09-08) | r3 |
| gabion `docs/census/registry-discovery-gabion.md` | `GB-` | `33540af1` (2026-09-07) | r1 (5,928 bytes) |
| linux-sources `census/registry-discovery-leg.md` | `LS-` (Q1–Q5) | `6cdfe79` (2026-09-07) | unstated |
| cassian-observability `docs/census-registry-discovery-leg.md` | `CO-` | `456e563` (2026-09-07) | r3, re-checked from r1 |
| rosettapkg `census/registry-discovery-leg.md` | `RD-` | `6141ff5` (2026-09-07) | r1, re-verified against r2 |

⚑ **linux-sources' worktree carries an AMENDMENT dated 2026-09-16 (A1: generator path moved to
`ls_gate/`; A2: the `skills` nomination confirmed and characterised) that is NOT in `6cdfe79` —
`git status` reports the file modified.** It is worktree testimony by the index's own §F rule and is
carried in the remainder (phase 2), not in `A`. The span below is built from the committed bytes.

**Normative arm** — paperkit's stated rule, *derive from the owner, do not re-enumerate a route
table with a second walk*, which the brief itself put under audit. ⚑ Two of five legs (GC, and the
dispatcher's declared stake in the index §X) carry a bias toward *"the walk is redundant"*; a third
(CO) runs both mechanisms and declares no stake. Agreement between GC and the dispatcher's thesis is
discounted below per the brief's r3.

**Not read:** paperkit's tree (no leg), substrate's and summit's uncommitted legs, cassian's
`scripts/census-registry-discovery.py` (claimed as part of the CO filing; its figures are taken
from the leg's own console blocks, not re-run).

## §A The span — what more than one leg holds, with the witness per row

⚑ **A ROW IS AN IDENTIFICATION, AND EACH CARRIES ITS WITNESS.** The witness types admitted: the same
third-party prose quoted in both · matching figures · an explicit cross-reference · the two legs'
own words saying the same thing about the same object, quoted side by side so a reader can refuse
the identification from the page. *"Obviously the same thing"* is not on the list.

| # | shared item | GC | GB | LS | CO | RD | witness |
|---|---|---|---|---|---|---|---|
| A1 | **the brief's DECLARE/DISCOVER binary has no cell for what the tree does** | Q1 "a third mechanism neither pole names — computed" | GB-1 "a third thing the question's binary does not name" | Q1 "NEITHER … the census's binary has no cell for this" | CO-1 "not a DECLARE repo or a DISCOVER repo. It is both" | RD-1 "Not the census's binary" | five legs, five quoted refusals of the binary. ⚑ **Identified only at the level of the refusal.** The five third forms are DIFFERENT and are NOT identified with each other — see §A-near |
| A2 | **a declared roster went stale against the tree, with a measured instance** | — (Q3 reports cost as an ordering constraint, not staleness) | GB-3 loader refuses: 4 anchors, control `lcr_sq_008` → 2 | Q3 "went stale LOUDLY, three times", each a gate refusal | CO-3 `t133` authored, never enrolled, reported passing | RD-3 `order` list drops a tally key; `PARTITION 92!=96` | four legs each quote a concrete instance from their own tree with the command. ⚑ LS's instance differs in KIND: its staleness was caught by a gate; GB/CO/RD's shipped silently. Row A2 identifies the EVENT (declaration drifted); A5 splits the outcome |
| A3 | **enrolment and execution are two things and the output cannot tell them apart** | — | — | — | CO-3 "a `--only` verification is a test of the ARM and never a test of its ENROLMENT" | RD-1 the pass line prints "the gates that ran, never the gates that were listed" | two legs, same distinction, opposite sides: CO names the defect, RD names the mechanism that forecloses it (print `$ran`). Cross-witness: RD-1(b)'s per-arm `ran` booleans are exactly the enrolment record CO-3 says was missing for `t133` |
| A4 | **an aggregate that agrees is not a property; per-member is the only count** | Q1 "reported one number for two different things" (55 modules vs 58 fragments) | GB-3 "a count over a disjunction is not a count per member" (`grep -c` over three anchors → 2) | — | CO-3 "the two counts agree today BY ACCIDENT, not by property" (90 = 90, nothing asserts it) | RD-3 a stale term "silently corrupted a second figure that was already proof-carrying" (7 → 3) | four legs, four measured instances where a total masked a per-member fact. Strongest row in the span: each was found by a second reading disagreeing with the first, none by inspection |
| A5 | **the walk's requirement is that there be no second place for the name to drift from** | Q2(c) "there is no central table to fall out of sync, because there is no central table" | — | — | CO-2 "the string `zram` appears exactly once in the tree — in the slice that implements it" | — | GC-2(c) ↔ CO-2: the same argument, stated as absence-of-a-second-source in one and as single-occurrence-of-the-name in the other. ⚑ This is the ONLY Q2 requirement two legs share, and it is the one the brief's Q2 was designed to elicit. It cuts against GC's own declared bias (GC says so) |
| A6 | **tolerance of an inconsistent registry: feature, or the absence of a gate — left open by the party that measured it** | Q2(b) "I do not know whether that tolerance is a feature or the absence of a gate, and this leg should not decide it" | GB-4 "the walk bought tolerance for a registry mid-edit, which is either a requirement or a bug"; GB-1a the validate-vs-tolerate axis | — | — | — | GC-2(b) ↔ GB-4: the identical open question, each explicitly declined. GB-1a supplies the vocabulary (population axis ⟂ trust axis); GC-2(b) supplies the 23-deletion instance. Identified as ONE open question, not two |
| A7 | **a reader written to answer this census is itself a re-derivation, filed against its author** | Q4 `scripts/registry_census.py` — "a third reader of the same association … filed against myself per §B1" | GB-4 `scripts/rederive_frontier.py` — "a second walk over a declared population … by the party that owns both" | — | CO-4 the reader `census-registry-discovery.py` moved its own count 17 → 18 (self-inclusion noted, not a re-derivation) | — | GC-4 ↔ GB-4: both wrote a fresh walk to measure a registry their tree already had a reader for, and both name it as the finding. CO is a near-miss (a reader artifact, not a re-derivation of the registry) |
| A8 | **`paperkit/routes.py` presence, as an absence claim with a positive control** | vendored byte-identical, pin fires on drift | `find src -name routes.py` → none; control finds `registry_loader.py` | `find` → one hit, own flake instrument; control: same predicate over `~/github/paperkit` → the engine's file; `pycodemod --calls leaves` → 0, control `warrant_modules` → 3 | name join 26 × 18 → 0; control: `verdict.py` lineage seen from docstring | `grep -rniE` → 0 in `.py`/`.toml`; control: `el-atlas` port found by the same reader | one present, four absent — and all four absences carry a §5 control. The row's value is the uniformity: this is the one question every leg answered with the same apparatus |
| A9 | **the brief was revised out from under the legs (r1 → r2, 5,928 → 8,560 bytes)** | (answered r3) | cites r1 at 5,928 bytes | — | measured 5928 → 8560; "the version this leg originally answered no longer exists" | measured 5928 → 8560; "re-verified question-by-question" | CO ↔ RD: byte-identical figures, independently measured the same morning (index §V rev 4 records CO's; RD's is in its own amendment). GB's 5,928 corroborates the r1 size. Both CO and RD conclude the same way: citation amended, findings stand |
| A10 | **every leg's negative carries a control (§5 compliance)** | comprehension control 8 → 8 (overturned its own first explanation) | `lcr_sq_008` → 2 | two controls (§A8) | `-type l` → 37 | `PHANTOM` plant; `el-atlas` | 5 of 5. ⚑ Two legs (GC, GB) record a control that FALSIFIED the author's first reading before filing — the §5 discipline working as designed, twice |

**Row count: 10.** Rows A3, A5, A6, A7, A9 are two-leg identifications; A1, A2, A4, A8, A10 span
four or five. ⚑ *Nothing above is a finding about which answer is right.* `A` is the index the glue
is taken over, and the brief's r3 warns that GC's and the dispatcher's agreement on the answer is
the flattery axis with a quorum — so A5, the one place GC's evidence cuts against GC's bias, is
weighted above any row where it does not.

## §A-near Near-misses — NOT identified, with the reason

⚑ **THESE LOOK ALIKE AND ARE KEPT APART.** Merging any of them would destroy a distinction a leg
was carrying, silently.

| candidates | why they rhyme | why they are NOT one |
|---|---|---|
| LS *discovered-then-frozen* ↔ RD *declared-then-witnessed* | both split the registry across two moments | opposite directions: LS discovers at build time and freezes to a static artefact so the closure is analysable; RD declares statically and witnesses at RUN time so the report names what executed. One moves toward static, the other toward dynamic |
| GB-1a *validating loader* ↔ LS-3 *`--check` gate* | both refuse on drift rather than tolerating it | GB's loader refuses CONTENT (a missing body anchor); LS's `--check` refuses MEMBERSHIP (a module added or removed). LS's uncommitted A2 makes exactly this split against `skills/check_generated.py` — carried in the remainder, but the distinction is already visible from the committed bytes |
| GC *computed routes* (`concepts_24` dict comprehension) ↔ LS *generated roster* | both are a third form, neither literal nor walked | GC's is computed at import from data already in the module; LS's is emitted to a file by a separate tool and gated. GC fire-tested the comprehension and found it was NOT the cause of its 8 orphans; LS's generator IS the mechanism. Different objects |
| CO-4 *verbatim port* (`verdict.py`) ↔ RD-4 *declared port* (`pm-depsort.py`) | both are code copied from a peer with the origin stated in the docstring | CO's stays BYTE-IDENTICAL on purpose (three repos share one record format); RD's DECLINES the upstream successors with a reason (they deleted machinery). One is an anti-fork, the other a recorded fork. Both legs say a port is the OPPOSITE of a re-derivation, and they are right for different reasons |
| CO-1 *five registries, mixed* ↔ GC-1 *two registries, mixed* | both trees hold declared and discovered side by side | CO's split is per-registry with a stated principle (a slice owns its name → discover; a label space → declare); GC's split is per-family with a stated history (the cotype walk exists for laziness, not because of the layout). The principles are not the same principle |
| GC-2(a) *lazy import* ↔ LS-2 *determinable import closure* | both are about imports and both are load-bearing | inverse requirements: GC needs to NOT import at registry-build time; LS needs the set of imports to be STATICALLY KNOWN. GC notes laziness is achievable in a declaration (`(fn, arg)` strings); LS notes the runtime sweep tainted 11 files. They would pull a shared design in opposite directions |

## §A-single Held by exactly one leg — carried, not glued, not dropped

These are not in `A` because nothing else holds them. They enter phase 2 as first-class remainder.

- **GB** — the disputed convening date gets a THIRD instrument reading (gabion's commit clock agrees
  with the harness). Still not adjudicated.
- **GB-2** — a declared table over a GENERATED population works when the generator is a field of
  each entry (`generated_artifact_manifest.yaml`, 46 artifacts / 107 paths).
- **LS-3** — a `--check` over a directory cannot see a warrant that was never given a file (`▣46`).
- **CO-2** — `--list` runs real code under discovery; `T138` asserts the enumeration is invariant
  under `env -i`. A cost of discovery no other leg names.
- **CO-5** — the brief's roster row for cassian is WRONG on contact ("symlinks the hooks"): eight
  regular files, zero symlinks, transition at `72d1b70` (2026-08-30), `--diff-filter=T`. Already
  relayed in the index §R; the apex records that it bears on Q1's prediction for that party.
- **RD-2** — the one place rosettapkg could have walked, it used `git ls-files` instead, because a
  glob's count "moves for reasons unrelated to the repo's content".
- **RD-4** — dependence on linux-sources' readers by subprocess, with the reader's rev and decoder
  digest carried in every scope stamp.
- **GC-4** — a NON-declaration (`consumer_fields` absent from `paper.toml`) dropped 30 field
  instances silently; "the highest-value item is not a re-derivation but a non-declaration".
- **GC** — the r4 revision itself: a filed leg's numbers drifted in every position after the tree
  moved, and the correction is recorded rather than applied silently.

## §P Phase 2 — PENDING

The glue along `A`, the remainder (three no-response legs, the dispatcher's unfiled leg, four
unrostered nominations, linux-sources' uncommitted amendment, and every §A-single row), and the
carried-testimony register are the next unit. ⚑ *If this file ends here when you read it, phase 2
has not run and the output is smaller than the largest leg — §2's own termination test says so.*
