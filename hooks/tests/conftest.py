# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 Mike Mol
"""Test-suite configuration for `mikemol-hooks`.

⚑ THIS FILE CARRIES NO FIXTURES YET, AND THAT IS ITS POINT. It marks `tests/` as a collection
root rather than a stray namespace directory, which is the half of the `INP001` answer that
belongs in the tree; the other half is the `namespace-packages` entry in `pyproject.toml`.
"""

from __future__ import annotations
